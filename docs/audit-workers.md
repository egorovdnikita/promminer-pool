# Аудит «Воркеров»: что править, сверху вниз

Сверка с файлом 🔵 **Воркеры [Web]** — `8mvRUmxWj1tT96Sb7Ou13h`, страница
`173:59219`, эталон — кадр «Подключили много воркеров» `173:59403`.
Замеры прототипа сняты из DOM при ширине 1920. Дата сверки: 13.09.2026.

Аудит всей страницы: главный экран сверху вниз (разделы 1–8), затем все
13 флоу и состояния (разделы 9–11). Разобранное прошлым заходом — сводка
хэшрейта, плитки статусов, лента групп и объединённая шапка таблицы —
в список правок не входит, эти блоки сходятся.

## 1. Строка действий

| Контрол | Макет | Прототип |
|---|---|---|
| «Серийные номера» | 204×**48**, радиус **16**, паддинг 0/**24**, белая | 187×40, радиус 12, паддинг 0/16 |
| «Подключить воркер» | 212×**48**, радиус **16**, паддинг 0/**24**, акцент | 205×40, радиус 12, паддинг 0/16 |
| Подпись кнопки | «Подключить воркер» | «Подключить воркера» |

Выбор монеты 110×48 без обводки — совпадает.

## 2. Сводка хэшрейта и плитки статусов

Совпадают: карточка 184, плитки 96. Правок нет.

## 3. Лента групп

Высота 49 против 48 в макете — разница от строки кнопки «Создать группу».
Мелочь, но в общий ритм не попадает.

## 4. Шапка таблицы

| Контрол | Макет | Прототип |
|---|---|---|
| Сегменты статусов | 847×48, радиус 16, элементы 44 с паддингом 0/16 | 793×48, элементы 44 — совпадает |
| «Фильтры» | 139×48, паддинг 0/**24** | 122×48, паддинг **12** |
| Поиск | 230×48, радиус 16 | совпадает |
| «Экспорт» | 136×**48**, радиус **16**, паддинг 0/**24** | 120×**40**, радиус **12**, паддинг 0/16 |

## 5. Таблица

| # | Что | Макет | Прототип |
|---|---|---|---|
| 5.1 | Высота строки | 48 | **59** |
| 5.2 | Паддинг ячейки | 12 | **15** |
| 5.3 | Ширины колонок | все данные по **153**, чекбокс **36**, последняя **68** | от 104 до 196, чекбокс 47, последняя 58 |
| 5.4 | Колонка «Модель» | значок производителя **16** перед названием | значка нет |
| 5.5 | Чипы тегов | высота **32**, паддинг 0/12 | высота 28 |
| 5.6 | Последняя ячейка | кнопка **36×36** с иконкой 16 | голая иконка, не кнопка и не кликается |

Высоту строки задирают именно чипы: 15 + 28 + 15 = 58. По макету ячейка
фиксирована на 48, а чип 32 в неё вписан — то есть вертикальный паддинг
у ячейки с тегами 8, а не 12.

### Логотипы моделей (5.4)

Макет ссылается на набор в дизайн-системе — `8rrL0jaovYD3jsrq7WF1L1`,
страница **Brands**, узел `148:49846`. Там 14 производителей майнеров:
`logo-bitmain`, `logo-whatsminer`, `logo-canaan`, `logo-IceRiver`,
`logo-AnexMiner`, `logo-Bitdeer`, `logo-Hummer`, `logo-Goldshell`,
`logo-Elphapex`, `logo-YAMI`, `logo-iPollo`, `logo-iBeLink`, `logo-Jasminer`,
`logo-Innosilicon`.

Понадобится таблица «модель → производитель»: у нас в парке S19/S21 (Bitmain)
и Q 90 — по названию производитель не выводится, нужно решить, что показывать.

## 6. Пагинация и подвал

Совпадают: пагинация 94 с паддингом 4/16/16/16, селект 110×48, страницы 40
с радиусом 12, подвал общий. Правок нет.

## 7. Поведение

- **Переход в деталку по клику на строку** — комментарий дизайнера в макете
  («При нажатии на всю строку происходит переход в деталку воркера»);
  в прототипе работает, проверено.
- **«По дефолту сортировка по шаре»** — заметка в макете (`290:157360`),
  не реализовано. Колонка «Отпр. шары» и в макете, и у нас показывает одно
  и то же значение во всех строках, поэтому сортировать пока нечего: нужны
  разные времена отправки.

## 8. Действия в строке и подсказки

Экшн-меню у воркера (`173:59614`, `173:59712`) — три пункта:
**«Изменить группы», «Изменить теги», «Удалить»**. У воркера в статусе
«Активен» или «Низкий хэшрейт» удаление заблокировано с подсказкой
«Вы не сможете удалить воркер в статусе "Активен" или "Низкий хэшрейт"».

В прототипе в последней ячейке просто значок `⋯` без меню, поэтому нет
ни пунктов, ни блокировки. Тултипы тегов и аптайма (`173:59516`) — только
аптайм, у тегов подсказки нет.

## 9. Флоу: что есть в макете и чего нет в прототипе

В прототипе три заглушки — `connect`, `group`, `filters`. Остальное
отсутствует целиком.

| Флоу | node-id | Что в макете | В прототипе |
|---|---|---|---|
| Подключение воркеров | `173:60724` | модалка «Подключение воркера» 550×1000, табы BTC и LTC, алерт «скопировано», скролл на 1440 | заглушка `connect`: три URL и два поля, без табов и алерта |
| Создание группы | `173:61615` | «Создать группу» 550, три состояния списка (группы есть / нет / много со скроллом), алерт, лимит **10 символов** на название | заглушка `group`: два поля, без списка и лимита |
| Редактирование группы | `173:67159` | «Создать группу» 550×576, лимит 10, кнопка сохранения гаснет при пустом названии | нет |
| Удаление группы | `173:67419` | «Удалить группу?» и «Удалить группы?» 550×362, выбор одной, нескольких и всех | нет |
| Привязка/отвязка групп | `173:64798` | «Изменить группы» 550, состояния: групп нет, много со скроллом, воркер привязан и не привязан, отвязка, алерт если выбор не изменился | нет |
| Создание тега | `173:62151` | «Создать тег» 550, три состояния списка, лимит **10** на название и **100** на описание | нет |
| Редактирование тега | `173:63756` | те же ограничения, кнопка гаснет при пустом названии, описание необязательное | нет |
| Удаление тега | `173:64237` | «Удалить тег?» 550×362, одна, несколько, все | нет |
| Привязка/отвязка тегов | `173:62936` | «Изменить теги» 550, те же состояния плюс переход в создание тега | нет |
| Фильтры | `173:65425` | **шторка 430×1080 справа**: «Теги» чипами (Без прошивки, Разогнан, Собран, Завершен, Подготовлен, Готов) и «Модели» (Bitmain, Canaan Avalon, Whatsminer, IceRiver, Bitdeer), поиск моделей, «ничего не найдено», сброс, пустое состояние | заглушка `filters`: модалка со статичной сеткой из шести полей |
| Удаление воркеров | `173:65944` | «Удалить воркер?» и «Удалить воркеры?» 550×362, только неактивные, тултип если среди выбранных есть активные | нет |
| Экспорт | `173:66473` | «Экспорт истории» 400×820: выбор монеты и периода, «часы работы» и «весь реестр» | кнопка выдаёт тост |
| Серийные номера | `173:66711` | **отдельный экран** 1920×1474 плюс модалки «Загрузить файл» 550: заполнение, ошибки, таб с ошибками, ввод серийника, выбор модели, поиск, ошибка размера файла, редактирование | кнопка в строке действий ни на что не ведёт |

## 10. Данные, которых не хватает под флоу

- **Теги** в прототипе захардкожены тремя значениями (`Разогнан`, `Готов`,
  `Без прошивки`) и не создаются. В макете их шесть и они пользовательские —
  нужен список в состоянии со счётчиками и привязкой к воркерам.
- **Группы** — то же самое: сейчас это фиксированный список городов
  в константе `GEO` со счётчиками, без создания, правки и привязки.
- **Модели** нужны с производителем: фильтр в шторке группирует по
  Bitmain / Canaan Avalon / Whatsminer / IceRiver / Bitdeer, и та же связка
  нужна под логотипы в таблице (пункт 5.4).
- **Статус воркера** должен запрещать удаление у «Активен» и «Низкий хэшрейт».

## 11. Не разобрано вовсе

- **Состояния главного экрана**: ховер с переходом в деталку (`196:76534`),
  тултипы тегов (`173:59516`), переключение на LTC (`1228:149263`),
  пустой экран (`173:59221`) и «несколько воркеров» (`173:59307`).
- **Деталка воркера** — отдельные страницы файла (`223:98541` десктоп,
  `223:98542` мобилка).
- **Адаптив 1440** (`173:60075`) и **мобилка** (`173:125851`).

## Что уже сделано

Отмечено по состоянию на 13.09.2026, после заходов по этому аудиту.

| Пункт | Статус |
|---|---|
| 1 — размеры кнопок строки действий | сделано |
| 3 — лента групп 49 | **не правим**: лишний пиксель даёт подчёркивание Segment Control Line из ДС, а не отступ. Ужимать контрол ниже 48 нельзя |
| 4 — шапка таблицы | сделано, плюс схлопывание в иконки на 1440 |
| 5.1–5.3, 5.5, 5.6 — строка, ячейки, ширины, чипы, кнопка действий | сделано |
| 5.4 — логотипы моделей | сделано: 5 логотипов выгружены в `public/logo-*.svg` |
| 7 — сортировка по шаре | сделано: время отправки стало разным, список сортируется по нему |
| 8 — экшн-меню и блокировка удаления | сделано |
| Подключение воркеров `173:60724` | сделано: вкладки BTC/LTC/ZEC, адреса, инструкция, контакты |
| Создание, правка, удаление групп | сделано одной модалкой |
| Создание, правка, удаление тегов | сделано, с описанием и палитрой |
| Привязка/отвязка групп и тегов | сделано, с алертом «выбор не изменился» |
| Фильтры `173:65425` | сделано: шторка 430 со всеми состояниями |
| Удаление воркеров `173:65944` | сделано, с тултипом на активных |
| Экспорт `173:66473` | сделано: меню из двух пунктов и модалка 400 |
| Серийные номера `173:66711` | сделано: экран, вкладка «С ошибками», модалка загрузки с ошибками размера и формата |
| Деталка воркера `223:98541` | сделано: шапка со связями, три карточки, график и события, модалки порога и параметров |

Логотипы выгружены на все 14 производителей из ДС, и `vendorOf()`
узнаёт каждого по названию модели — значок в таблице находится для любой
модели, а фильтр по-прежнему показывает пять брендов из макета.

Осталось: мобилка.

Известное упрощение: подписи делений графика масштабируются вместе с svg,
поэтому точны на 1920 и чуть мельче на 1440 — так же ведёт себя график
на Главной. Чтобы кегль не зависел от ширины, деления надо выносить
из svg в HTML — отдельная правка для всех графиков сразу.

Мобилка разобрана отдельно — [audit-workers-mobile.md](audit-workers-mobile.md).

## Второй заход: состояния десктопа (13.09.2026)

Первый аудит разбирал эталонный кадр «Подключили много воркеров».
Здесь — остальные кадры секции «Состояния Главного экрана воркеров»
(`173:59220`), которые тогда остались без сверки, и адаптив 1440
(`173:60075`). Замеры прототипа сняты на 1920 и 1440.

### A. Экшн-меню в строке — четыре пункта, а не три

Проверил три кадра с открытым меню (`173:59516`, `173:59614`,
`173:59712`) — во всех оно одинаковое:

| Что | Макет | Прототип |
|---|---|---|
| Пункты | **«Посмотреть»**, «Изменить группы», «Изменить теги», «Удалить» | три: без «Посмотреть» |
| Размер | 169×**192**, пункт 145×40 | 188×163, пункт 40 |

Первый аудит зафиксировал три пункта — это была моя ошибка: я смотрел
кадр, где верхний пункт не попал в выборку. «Посмотреть» дублирует
переход по клику на строку, но в макете он есть.

### B. Пустой парк (`1228:149263`)

Кадр подписан «Переключили монету на LTC», но в селекторе монеты там
**ZEC** и единица **KSol/s** — то есть пустой парк показан на третьей
монете. Ось `coin` с ZEC и KSol/s в прототипе это подтверждает.

| # | Что | Макет | Прототип |
|---|---|---|---|
| B1 | Текст пустого состояния | «Подключите воркеры, чтобы начать майнить» + «Ваши устройства появятся в течение 10 минут после подключения» | «Воркеров пока нет» + «Подключите первый воркер, чтобы увидеть статистику по парку» |
| B2 | Кнопка в пустом состоянии | **«Подключить воркер»** | кнопки нет |
| B3 | Строка действий | только выбор монеты и «Подключить воркер» | остаётся ещё «Серийные номера» |
| B4 | Шапка таблицы | только сегмент статусов | остаются «Фильтры», поиск и «Экспорт» |

### C. «Нулевое состояние, когда ничего не добавлено» (`173:59221`)

Это **не пустой парк**: воркеры есть и данные заполнены, но не заведено
ни одной группы и ни одного тега. Лента групп сходится — «Все» и
«Создать группу». А вот колонка «Мои теги» в макете во всех строках
показывает **«—»**, в прототипе там по-прежнему теги: колонка берёт их
из константы и не смотрит на список заведённых.

### D. «Подключили несколько воркеров» (`173:59307`)

В макете **пять** строк, в прототипе сценарий «Мало записей» даёт три.
Мелочь, но кадр рисует именно пять — с разными тегами и «+1».

### E. Адаптив 1440 (`173:60076`, `173:60180`)

| # | Что | Макет | Прототип |
|---|---|---|---|
| E1 | Колонки таблицы | держат шаг **145** и таблица **прокручивается вбок** (кадр `173:60180` — та же таблица, сдвинутая на 231) | колонки ужимаются до 103, прокрутки нет |
| E2 | Сегмент статусов | «Низкий хэшрейт» сокращается до **«Низкий хэш»** | подпись остаётся полной |

Остальное на 1440 совпадает: сводка 1190×274, плитки 286, лента групп.

### F. Ховер и тултипы — совпадают

Кадр `196:76534`: у чипа «+N» подсказка со скрытыми тегами
(«Без прошивки, Готов, Завершен») — в прототипе сделано. Подсказка
у «Uptime» в шапке таблицы — тоже на месте.

### Сделано по второму заходу

Правки внесены 13.09.2026, проверено на 1920 и 1440.

| Пункт | Что стало |
|---|---|
| A | В экшн-меню первым пунктом «Посмотреть» — тот же переход, что и клик по строке; меню сузилось до 169 |
| B1–B2 | Пустое состояние переписано: «Подключите воркеры, чтобы начать майнить», «Ваши устройства появятся в течение 10 минут после подключения» и кнопка «Подключить воркер» |
| B3 | На пустом парке в строке действий остаются только монета и «Подключить воркер» |
| B4 | На пустом парке шапка таблицы — только сегмент статусов, без фильтров, поиска и экспорта |
| C | Колонка «Мои теги» показывает «—», когда теги не заведены |
| D | Сценарий «Мало записей» даёт пять воркеров |
| E1 | Таблица не сжимается ниже 1554: колонки держат 145 и уезжают вбок (на 1920 растягиваются до 151, как в макете) |
| E2 | Сегмент сокращает «Низкий хэшрейт» до «Низкий хэш» на окнах уже 1450 |

Высота меню доведена до макетных 192 явным `height`: четыре пункта по 40
с шагом 8 в поле 169×192.

## Третий заход: флоу и модалки (13.09.2026)

Первые два захода разбирали экран. Здесь — сами модалки, которые
были собраны по макетам, но ни разу не сверены по геометрии.
Замеры сняты на окне 1920×1080.

### Ф1. Максимальная высота модалки — общее правило

В макетах трижды повторена заметка (`173:61614`, `173:62150`, `173:62859`):

> Максимальная высота модалки относительно высоты окна браузера =
> высоте браузерного окна − 35 px от верхней границы и − 35 px от нижней

То есть `calc(100vh - 70px)`. В прототипе стоит `86vh`. На окне 1080
это **929 против 1010**, и модалки режутся раньше времени:

| Модалка | Макет | Прототип |
|---|---|---|
| Подключение воркера | 550×**1000** | 550×929, дальше прокрутка |
| Создать группу, много групп | до 550×**1010** | 550×929 |
| Создать тег | 550×**966** | 550×929 |

Правка одна — в `.modal` и отступе маски.

### Ф2. Подзаголовок живёт в шапке, а не в теле

`Modal HeaderA` в макете — **92** по высоте: паддинг 24 сверху и 16 снизу,
внутри заголовок 30, зазор 4 и подзаголовок 18. В прототипе шапка 80,
а подзаголовок вынесен в тело с отступом 18 снизу.

Из-за этого **все модалки с подзаголовком выше макета на 22–24**:

| Модалка | Макет | Прототип |
|---|---|---|
| Настроить уведомления | 550×334 | 550×**358** |
| Добавить заводской номер | 550×452 | 550×**474** |
| Загрузить файл | 550×418 | 550×**500** |

У «Загрузить файл» разрыв больше: там ещё и отступы вокруг заметки
про актуальную таблицу.

### Ф3. Подтверждения удаления собраны не по макету

Кадры `173:65945` (воркер), `173:65952` (воркеры), `173:67539` (группа),
`173:67546` (группы) устроены одинаково: **550×362** с иллюстрацией.

| # | Что | Макет | Прототип |
|---|---|---|---|
| Ф3.1 | Ширина | **550** | 500 (`size:'sm'`) |
| Ф3.2 | Верх модалки | **ImageContainer 550×148** с картинкой 542×144, крестик поверх неё | иллюстрации нет, сразу заголовок |
| Ф3.3 | Заголовок | 24 SemiBold на y172, под картинкой | 24 SemiBold в обычной шапке |
| Ф3.4 | Кнопки | две по **247×56** в ряд | 223×56 — следствие ширины 500 |

Приём в прототипе уже есть: `obsdel` и другие подтверждения профиля
рисуются с `img:'/modal-delete.png'`. Удаления воркеров, групп и тегов
просто не переведены на него.

### Ф4. В подтверждениях удвоен подвал

У `wkdel`, `wkdels`, `gdel` и `tdel` не выставлен `acts:false`, поэтому
компонент рисует **две строки кнопок**: свою «Отменить / Удалить»
и стандартную «Подтвердить / Отмена». Четыре кнопки вместо двух —
видно на любом из четырёх подтверждений.

Это не расхождение с макетом, а поломка.

### Что сошлось

- Ширина 550 у всех остальных модалок, крестик 40 кругом с подложкой
  `--ctrl` — как в ДС.
- Подключение воркера: три вкладки BTC / LTC / ZEC, пять строк с копированием,
  четыре шага инструкции, контакты — состав совпадает.
- Менеджеры групп и тегов: шапка 80 без подзаголовка, кнопка создания 56,
  список карточками 64 с шагом 4 — совпадает.
- Подвал 104 с паддингом 24 и кнопками 56 — совпадает везде, кроме Ф3.4.

### Сделано по третьему заходу

Правки внесены 13.09.2026, замеры перепроверены на 1920×1080.

| Пункт | Что стало |
|---|---|
| Ф1 | `max-height: calc(100vh - 70px)` и отступ маски 35 — по заметке из макета. Подключение воркера встало в 550×1001 при макетных 1000 |
| Ф2 | Подзаголовок переехал в шапку: `.mhead` стала колонкой «заголовок + подпись», шапка выросла до макетных **92**. У модалок с иллюстрацией подпись осталась в теле — так в макете |
| Ф3 | Подтверждения удаления воркера, воркеров, группы и тега переведены на вариант с иллюстрацией: 550×**362**, картинка 148, кнопки 247×56 в ряд |
| Ф4 | Выставлен `acts:false` — вместо четырёх кнопок снова две |

Высоты после правок:

| Модалка | Макет | Прототип |
|---|---|---|
| Удалить воркер | 550×362 | 550×**362** |
| Настроить уведомления | 550×334 | 550×**334** |
| Добавить номер и модель | 550×452 | 550×450 |
| Загрузить файл | 550×418 | 550×420 |
| Подключение воркера | 550×1000 | 550×1001 |

Попутно: класс `.note` для заметки в модалке загрузки совпадал с карточкой
уведомления и делал её флекс-контейнером в три строки — переименован
в `.mnote`. Это второй такой случай после `.xs`, стоит проверять имена
по всему `app.css`, прежде чем заводить новые.

## Порядок работ

1. Механика главного экрана — пункты 1, 4, 5.1–5.3, 5.5, 5.6: размеры кнопок,
   строка таблицы 48, ширины колонок, чипы, кнопка действий. Один заход.
2. Экшн-меню в строке с блокировкой удаления (пункт 8) — оно открывает
   половину флоу.
3. Теги и группы как данные (пункт 10) — без них флоу привязки, создания,
   правки и удаления делать не на чем.
4. Шторка фильтров (`173:65425`) — самый заметный флоу, и он опирается
   на теги с моделями из шага 3.
5. Дальше по убыванию: удаление воркеров, экспорт, подключение, серийные
   номера отдельным экраном.
6. Логотипы моделей (5.4), деталка, адаптив и мобилка.
