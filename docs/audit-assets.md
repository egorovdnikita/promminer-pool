# Аудит: «Мои активы»

Сверка с файлом 🔵 **Мои активы [Web]** — `cMs238oAwQH0l0rAJSyt3Z`, страницы
«Главный экран Активов» `1:3`, «Вывод и продажа» `223:132579`,
«Кошелек и расчетный счет» `106:68852`. Дата сверки: 14.09.2026.

> В файле каждая секция лежит **дважды** (например `1:12438` и `1:9882`) —
> содержимое совпадает, берём первую.

## Главное: вкладка меняет не подпись кнопки, а половину таблицы

В прототипе сегмент-контрол «Выплаты / Продажи» переключает только текст
кнопки в последней колонке. В макете вкладки называются **«Вывести» /
«Продать»** и меняют три колонки из восьми:

| Вкладка | Колонки 5–7 | Кнопка |
|---|---|---|
| Вывести (`95:19946`) | Кошелек · Порог автовыплат ⓘ · Автовыплаты ⓘ | Вывести |
| Продать (`212:107049`) | Расчетный счет · Порог продажи ⓘ · Условия ⓘ | Продать |

Порог автовыплат задаётся в монете (`0,001 BTC`), порог продажи — в рублях
(`10 000 ₽`) и одинаков для всех монет.

## 1. Плашка баланса (`137:54797`)

Отдельный блок **над** карточкой, а не внутри неё: 1652×154, фон `#7086fc`,
радиус 24, паддинг 24, два белых декоративных круга (318 и 866).

| Что | Макет | Прототип |
|---|---|---|
| «Общий баланс» | 20/24 SemiBold | 14 |
| Сумма | **56/64** SemiBold | 40 |
| «≈ … ₽» | 20/24 SemiBold, по нижнему краю суммы | 16, по центру |
| Плитки | 173×106, `#ffffff26`, r24, паддинг 16/24 | 150 мин., паддинг 14/16 |
| Иконка плитки | 32, без подложки | 36 в подложке `#ffffff29` |
| Подпись плитки | 20 SemiBold | 16 |
| Стрелка | **Alt Arrow Right** 24 | шеврон вниз |

Иконки плиток: «Доход» — `Money / …` (32), «Выплаты» — `Money / Card Recive`.

## 2. Карточка с таблицей (`95:19946`)

- Карточка: паддинг **8**, шапка `Title` 1636×80 с паддингом 16.
- Segment Control 201×48 — размер M, как и везде.
- Шапка таблицы: ячейки `#f3f4f6`, паддинг 12, зазор 4, подписи **14 SemiBold
  `#9ca3af`**; иконки валют (24) стоят **перед** подписью у «Баланс, $»
  и «Баланс, ₽», иконка сортировки — после «Баланс», ⓘ (16) — после
  «Порог автовыплат» и «Автовыплаты».
- Строки 48, ячейка паддинг 12, зазор 8, значения **14 SemiBold `#111827`**.
- Восемь колонок по 205.

### Ячейки

| Колонка | Макет |
|---|---|
| Монеты | иконка 24 + тикер, зазор 4 |
| Кошелек / Расчетный счет | чип **26** высотой: маскированный адрес + иконка копирования 16; если адреса нет — чип с плюсом и «Добавить» цветом `#7086fc` |
| Порог | чип 26: значение + карандаш 16. У DOGE и ZEC на вкладке выплат — просто текст (паддинг 12/20) |
| Автовыплаты / Условия | Switch 40×24, включённый `#7086fc` |
| Действие | кнопка **104×36**, паддинг 0/24, 14 SemiBold |

В прототипе: «Добавить» всегда, порог — пилюля 32 высотой, кнопка `.btn.sm`.

### Четвёртая монета

В макете четыре строки — **BTC, LTC, DOGE, ZEC**. В прототипе три.

## 3. Подсказки ⓘ

| Где | Текст |
|---|---|
| Порог автовыплат (`231:129971`) | Выплаты возможны при достижении минимального порога автовыплат |
| Автовыплаты (`231:130644`) | Переводы выполняются раз в сутки с 14:00 до 17:00 (UTC). До достижения порога средства накапливаются на вашем балансе |
| Порог продажи (`231:131328`) | Выплаты возможны при достижении минимального порога |
| Условия (`231:131978`) | При продаже ЦВ взимается комиссия в соответствии с условиями оферты |

## 4. Уведомление о верификации (`212:107049`)

На вкладке «Продать», пока не выполнены условия — синий алерт:

- заголовок «Для продажи цифровой валюты» 16 SemiBold `#3b82f6`;
- текст «Добавьте ФИО/Наименование организации и загрузите выписку из реестра
  майнеров в разделе» 14 Medium `#6b7280`;
- ссылка «Верификация» 14 SemiBold `#7086fc`.

## 5. Состояния экрана

| Состояние | Кадр | Что видно |
|---|---|---|
| Нулевой баланс | `221:152360` | всё по нулям, у всех монет «Добавить» |
| Ошибка загрузки | `223:33637` | «Не загрузилось» / «Попробуйте обновить» / кнопка «Обновить» |
| Скелетон | `223:35837` | заглушки вместо контента |

Ошибки загрузки в прототипе нет ни на одном экране — ось `load` знает
только «Загружено» и «Скелетон».

## 6. Модалка «Вывод средств» (`226:124696`, 550×520)

Три шага, прогресс-полоса из трёх сегментов.

**Шаг 1.** Поле «Вывести» (контейнер 502×106, r24, паддинг 16): сумма +
селект монеты 110 справа; под полем строка «Минимум 0,001» / «Максимум
0,0411118179» (12 SemiBold `#9ca3af`). Ниже кнопка 502×40 r12 «Вывести все
0,0411118179 BTC». Ниже блок «на Кошелек» 502×78 r16 с маскированным
адресом 16 Medium. Внизу «Время зачисления может составлять до 24 часов».
Кнопки «Отменить» / «Подтвердить».

**Ошибки** (окно вырастает до 580): «Поле обязательно для заполнения»
(`238:140186`), «Сумма меньше порога выплаты» (`274:48952`).

**Шаг 2** (`239:145355`) — «Введите код», «Мы отправили код подтверждения
на указанный вами номер», номер, «Запросить новый код через 00:59»,
кнопки «Отменить» / «Вывести».

**Шаг 3** (`239:146174`) — «Заявка на вывод средств создана», «Статус заявки
можете посмотреть в разделе **Выплаты**», кнопка «Отлично».

Смена монеты в селекте обнуляет заполненные поля (`239:140957`); монеты,
недоступные для вывода, в списке задизейблены (`236:16288`).

## 7. Модалка «Продажа средств» (`242:57978`, 550×746)

Два связанных поля: «Продать» (монета, максимум 0,039, кнопка «Продать все
0,039 BTC») и «Получить» (RUB, минимум 10 000). Значения пересчитываются
друг из друга (`243:62148`). Ниже «Расчетный счет 2345****1468»,
«По курсу: 1 BTC ≈ 9 731 040,34 ₽» и три примечания:

- Время зачисления может составлять до 24 часов в рабочие дни
- Заявка будет обработана в рабочие дни с 10:00 до 18:00 (по Москве)
- При продаже ЦВ взимается комиссия в соответствии с условиями оферты

Ошибки — 810. Шаг 2 — код, кнопка «Продать». Шаг 3 — «Заявка на продажу
средств создана».

## 8. Модалка «Порог автовыплат» (`246:44137`, 550×444)

Поле «Порог автовыплат» + селект монеты, «Минимум 0,001», подпись «Выбрать
порог автовыплат:» и чипы `0,001 / 0,01 / 0,05 / 1 / 5`. По умолчанию выбран
минимальный; при ручном вводе все чипы снимаются (`287:164300`). Ограничение
поля: BTC — 10 знаков после запятой, LTC — 4 (`246:176423`). Успех —
тост «Порог автовыплат успешно изменен».

## 9. Модалка «Добавить адрес» (`106:68854`, 550×510)

Поле «BTC-адрес» с кнопкой «Вставить», список допустимых форматов и
предупреждение «В целях безопасности выплаты будут заморожены на 3 дня.
Доход будет накапливаться…».

| Монета | Допустимые адреса | Высота |
|---|---|---|
| BTC | P2PKH (Legacy), P2SH-P2WPKH (SeqWit), P2WPKH (SeqWit Bech32), P2WSH (SeqWit Bech32), P2TR (Taproot Bech32m) | 510 |
| LTC | P2SH-P2WPKH (SeqWit) | 474 |
| DOGE | P2PKH (Legacy), P2SH | 492 |
| ZEC | ссылка «Узнать» | 474 |

Ошибки поля: «Поле обязательно для заполнения», «Некорректный адрес».
Ошибки кода: «Введен неверный код», «Код недействителен. Запросите новый».
Успех — «Адрес успешно добавлен» и повтор предупреждения про 3 дня.

Число шагов зависит от привязанных подтверждений (`210:91673`): телефон —
3 шага, телефон + Google Authenticator — 4, почта — 3, почта + GA — 4.
Правило выбора канала: есть телефон, почта и GA → почта + GA; есть телефон
и почта → почта.

## 10. Модалка «Изменить адрес» (`211:131764`, 550×660 / 756)

То же плюс блок **«История изменений»** — таблица «Адрес / Добавлен /
Активирован» с маскированными адресами и датами; высота окна растёт
с числом строк.

## 11. Модалка «Добавить счет» (`212:108483`, 550×728)

Поля «Выберите банк» (селект), «БИК» с подсказкой «Введите 9 цифр кода
своего банка», «Расчетный счет» с кнопкой «Вставить». Внизу блок «Нужна
помощь c открытием счета?» со строками «Почта support@pmpool.ru» и
«Telegram @PoolSupport». Кнопки «Отменить» / «Добавить».

## Сделано

Проверено замерами DOM при 1920: плашка 1641×154, карточка 336 (в макете 1652×154
и 336), строка таблицы 48, кнопка действия 104×36, окна модалок 520 / 746 / 444 /
510 / 660 и 580 / 810 с ошибкой — совпадают с макетом, внутренней прокрутки нет.

| # | Правка |
|---|---|
| 1 | Плашка баланса вынесена из карточки: 154, сумма 56/64, плитки 173×106 со стрелкой Alt Arrow Right |
| 2 | Вкладки «Вывести / Продать» меняют три колонки из восьми |
| 3 | Добавлена ZEC; кошельки, пороги и максимумы вынесены в данные |
| 4 | Чипы адреса и порога 26, свитч автовыплат, кнопка 104×36, строка 48, колонки равной ширины |
| 5 | Четыре подсказки ⓘ с текстами из макетов |
| 6 | Алерт верификации на вкладке продажи со ссылкой в раздел |
| 7 | Ошибка загрузки — третье значение оси `load`, общий экран «Не загрузилось» |
| 8 | Модалки: вывод и продажа в три шага, порог с чипами, адрес и изменение адреса с историей |
| 9 | Счёт открывает существующую модалку `vaccm` — она уже совпадает с макетом 212:108483 |
| 10 | Монеты без кошелька задизейблены в селекте вывода (236:16288) |
| 11 | Оси сценариев: «Кошельки», «Автовыплаты», «Ошибка в поле»; высота окна умеет зависеть от состояния |

## Не разобрано

- Мобильный файл «Мои активы (Mobile)» `csrFfmOTcTwytBVFjLdO3v` — отдельный этап.
- Ветвления подтверждения (3/4 шага, почта против SMS, Google Authenticator)
  сделаны одним путём: прототип всегда показывает один шаг с кодом.
- Операции со счётом «+ подтверждение» (`221:138176`) — сделано без кода.
- Изменение счёта (`213:123629`) и удаление кошелька в макетах есть,
  в прототипе пока нет.
