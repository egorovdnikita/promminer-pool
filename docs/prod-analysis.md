# Разбор прода pool.promminer.ru

Что удалось снять с публичной части продукта (страницы `/login`, `/register`, `/restore`
и собранные ассеты). Авторизованная часть закрыта, поэтому внутренние экраны сверялись
по макетам, а с прода взяты фактические значения дизайн-системы, шрифты, логотип и адреса.

## Стек

Vue 3 + Vite (rolldown) + Tailwind + PrimeVue, иконки — собственный набор `svg-icons`
(имена в проде вырезаны минификатором). Аналитика — Яндекс.Метрика, поддержка — HelpDeskEddy.

## Роутинг

| Путь | Назначение |
|---|---|
| `/login`, `/register`, `/restore`, `/auth` | группа входа |
| `/r/<referral_code>` | реферальная ссылка; код кладётся в `localStorage` под ключи `referral_code` и `refferal_code` |
| `/watcher/<token>/<любой путь>` | **режим наблюдателя** — весь набор внутренних маршрутов дублируется с префиксом токена (`duplicateRoutes.js`), имена роутов получают префикс `token-`, в `meta.hasToken = true` |
| `/404`, `/:catchAll(*)` | не найдено |

Токен наблюдателя определяется по третьему сегменту пути длиной > 30 символов; обычный
пользователь ходит с токеном из `localStorage`.

## Внешние адреса (из сборки)

```
API                 https://pmpool.ru
Сайт / ЛК           https://pool.promminer.ru/
База знаний         https://pmpool.ru/blog/
  IP майнера        /blog/asic-kak-nayti-ip-adres-maynera/
  Заводской номер   /blog/gde-nayti-zavodskoy-nomer-mayneraa/
Документы           https://files.promminer.ru/docs/
Бот с алертами      https://t.me/PromminerAlertbot
Поддержка           poolsupport@promminer.ru
Удаление аккаунта   https://promminer.ru/delete-account/
```

Документы: Публичная Оферта, Политика конфиденциальности, Пользовательское соглашение,
Политика соглашения на обработку персональных данных, Реквизиты, Политика обработки cookie.

## Дизайн-система (фактические значения из `assets/css/index-*.css`)

Тёмная тема на проде включается классом `.dark` на корне; в прототипе — `[data-theme="dark"]`
с тем же набором переопределений.

### Палитра

```
indigo   400 #7086fc  500 #6366f1  600 #4f46e5      ← accent (light / dark / hover)
neutral   50 #fafafa  100 #f5f5f5  200 #e5e5e5  300 #d4d4d4
         400 #a3a3a3  500 #737373  600 #525252  800 #262626  900 #171717  950 #0a0a0a
green    500 #22c55e  600 #16a34a    amber 500 #f59e0b  600 #d97706
red      500 #ef4444  600 #dc2626    brand (знак логотипа) #5369e0
alpha-рампы: 008 = +14, 012 = +1f, 016 = +29, 024 = +3d, 032 = +52, 048 = +7a, 064 = +a3
             (neutral-alpha строится от #737373, indigo-alpha — от #6366f1)
```

### Семантика (light → dark)

| Токен | Light | Dark |
|---|---|---|
| bg-base-primary | neutral-100 | neutral-950 |
| bg-base-secondary / elevated | white | neutral-900 |
| bg-base-tertiary | neutral-200 | neutral-800 |
| bg-base-overlay | alpha-black-064 | alpha-black-064 |
| content-base-primary | neutral-900 | neutral-50 |
| content-base-secondary | neutral-500 | neutral-400 |
| content-base-tertiary | #9ca3af | neutral-500 |
| border-base-container / divider | neutral-alpha-016 | neutral-alpha-016 |
| control-primary | indigo-400 | indigo-500 |
| control-primary-hover | indigo-500 | indigo-600 |
| control-neutral | neutral-alpha-012 | neutral-alpha-016 |
| border-focus | indigo-alpha-048 | indigo-alpha-048 |

### Размеры

> Где у токена два значения, это **mobile → desktop** (проверено по таблице
> «Radius» в дизайн-системе: `Radius-l` = 16 на мобиле и 24 на десктопе).
> Парами light/dark идут только цвета — они лежат в блоке `.dark`.
> Имена в прототипе совпадают с DS: `--r-3xs … --r-2xl`, `--r-f`.

```
radius   2 4 6 8 12 16 24 32 48 999      base: 3xs 2, 2xs 4, 1xs 6, xs 8, s 12, m 16, l 16→24, xl 24→32
size     24 28 32 36 40 44 48 56 64      base: 2xs 24, xs 28, s 32, m 36, l 40, xl 44, 1xl 48, 2xl 56, 3xl 64
spacing  2 4 8 12 16 20 24 28 32 40 48 60 72
модалка  ширина 375 (mobile) → 600 (desktop), высота до 720
```

### Типографика (Gilroy, mobile → desktop)

Семейство одно — Gilroy и для текста, и для заголовков. Начертания: Thin 100,
ExtraLight 200, Light 300, Regular 400, Medium 500, SemiBold 600, Bold 700,
ExtraBold 800, Black 900.


```
title-1     56/64 → 72/80        headline-4  20/26 → 24/30
title-2     48/56 → 64/72        headline-5  16/20 → 20/24
title-3     48/56 → 56/64        headline-6  16/20 → 20/24
headline-1  40/48 → 48/56        body-1 18/22   body-2 16/20   body-3 14/18   body-4 12/16
headline-2  32/40 → 40/48        caption-1 12/16  caption-2 10/12
headline-3  28/34 → 32/38        label-1 16/20 (-0.18)  label-2 14/18 (-0.16)  label-3 12/16 (-0.12)
веса        100 200 300 400 500 600 700 800 900
```

## Экраны входа (сняты с DOM)

- Лист белый (`bg-white`), колонка `max-width: 343px` на мобиле / `358px` на десктопе, по центру.
- Логотип сверху по центру: знак 24×24 `#5369E0` + начертание 114×22 `#111827` + «POOL» 36×16 `#7086FC`.
- Заголовок 32/38 SemiBold по центру, подзаголовок body-2 по центру.
- Поле: высота 56, radius 16, фон `#6b72801f`, паддинги 12, gap 12, placeholder SemiBold `#9ca3af`;
  у пароля — кнопка-глаз справа.
- Кнопка: `w-button--xl` — высота 56, radius 16, `#7086fc`, 16/20 SemiBold, во всю ширину,
  `opacity .5` пока не приняты согласия.
- Три чекбокса согласий с ссылками на PDF; «Забыли пароль?» — ссылка справа над ними.
- Внизу — «© 2026 Promminer Pool. Все права защищены».
- Заголовки вкладок: «Авторизация — Promminer», «Регистрация — Promminer»,
  «Восстановление пароля — Promminer».
- В сборке есть `useOtpClipboard` и `usePhoneMask` — значит после регистрации/восстановления
  идёт шаг с кодом из СМС и маска телефона.

## Сверка по скриншотам авторизованного аккаунта

Внутренние экраны прода (аккаунт `pminer54055`) сверялись по скриншотам. Что уточнилось:

### Сайдбар

Иконки разделов отличались от того, что было в прототипе:

| Раздел | На проде |
|---|---|
| Воркеры | кирка (было — процессор) |
| Финансы | банковская карта (был — кошелёк) |
| Отчет о майнинге | круговая диаграмма (был — документ) |
| Инструменты | калькулятор (были — ползунки) |
| Мониторинг | столбчатая диаграмма (был — монитор) |

### Хедер

- **Пилюля с балансом кликабельна** и открывает попоувер «Сводка по аккаунтам»:
  общий баланс, количество аккаунтов, затем карточка на каждый аккаунт
  (баланс, хэшрейт BTC, хэшрейт LTC) и кнопка «Добавить суб-аккаунт».
- **Меню профиля**: шапка с аватаром, именем и кнопкой копирования; пункты с иконками
  в порядке «Мой профиль, Безопасность, Верификация и реквизиты, Центр суб-аккаунтов,
  Наблюдатели, Уведомления», разделитель, «Настройки», разделитель, «Выйти»
  (нейтральным цветом, не красным).
- **Попоувер уведомлений** широкий: заголовок «Новые уведомления» и «Прочитать все»
  сверху, список, внизу кнопка «Посмотреть все» во всю ширину; пустое состояние — иллюстрация.

### Личный кабинет

Заголовок раздела — «Личный кабинет» (не «Профиль»), одинаковый для всех вкладок.
Вкладки: Мой профиль · Безопасность · Верификация и реквизиты · Центр суб-аккаунтов N ·
Наблюдатели N · Уведомления — со **счётчиками** и **белой активной** вкладкой (не лавандовой).

Сводка профиля состоит из:

- «Мои суб-аккаунты» — карточки аккаунтов (баланс, хэшрейт BTC/LTC, четыре счётчика
  воркеров цветными точками) и плитка «Создать суб-аккаунт»;
- «Мои наблюдатели» — карточки со значками монет, тегом доступа, сроком, полем ссылки
  с копированием и кнопкой QR-кода; плитка «Добавить наблюдателя»;
- справа «Мой профиль» — аватар, имя с копированием и карандашом, строки «Последняя сессия»
  и «Google Authentication» со статусом, и «Последние уведомления».

### Мои активы

- Герой: «Общий баланс», крупная сумма в $ и «≈ … ₽» рядом; справа две плитки-действия
  «Доход ›» и «Выплаты ›» с иконками.
- Вкладки называются **«Выплаты / Продажи»** (не «Вывести / Продать»), при этом кнопка
  в строке таблицы — «Вывести».
- В таблице настоящие логотипы монет, у колонок «Баланс, $» и «Баланс, ₽» — круглые значки
  валют, «+ Добавить» это кнопка с подложкой, порог автовыплат — пилюля с карандашом.
- Пороги по умолчанию: `0.001 BTC`, `0.001 LTC`, `1 DOGE` (через точку).

### Футер

Пять ссылок: Оферта · Политика конфиденциальности · Пользовательское соглашение ·
Политика обработки персональных данных · Реквизиты. Ссылки на cookie-политику в футере нет.

### График

Подпись оси — «Хэшрейт, TH/s» полностью (в прототипе обрезалась до первой буквы),
справа — «Реджект, %».

### Компоненты выбора

В библиотеке «💙 Дизайн-система» два компонента для взаимоисключающего выбора:
**Segment Control** (контейнер с подложкой, активный сегмент белый) и
**Segment Control Line** (подчёркивание вместо подложки). В прототипе им соответствуют
`seg()` и `segLine()`; чипы остаются только для фильтров и множественного выбора.

> **Как получить id страницы.** `get_metadata` без `nodeId` возвращает только первый
> элемент верхнего уровня, а если первым идёт разделитель страниц (как `🟢 Макеты
> в разработку`), то остальные страницы в перечисление не попадают вовсе. Рабочий способ:
> открыть файл в браузере, правый клик по нужной странице в панели Pages →
> **Copy link to page**, из ссылки взять `node-id`. Дальше `get_screenshot` по этому id
> отдаёт страницу целиком.
> Известные id: «🔵 Профиль [Web]» → «Сводка профиля» = `451:6014`,
> «Действия с телефоном» = `531:19524`.
>
> ⚠️ Автоматизировать сбор id кликами по панели Pages не стоит: синтетическое
> перетаскивание в этой панели меняет порядок страниц в файле. Один раз так и вышло —
> порядок пришлось возвращать через Cmd+Z. Надёжнее взять ссылки руками.
>
> **Как читать макеты через Figma MCP.** Сервер отдаёт содержимое только того файла,
> который открыт в десктоп-приложении Figma, и только его активную страницу.
> Для закрытых файлов `get_metadata` возвращает страницу `0:1` — она пустая, реальные
> макеты лежат на других страницах. Поэтому порядок такой: открыть нужный файл в Figma →
> `get_metadata` без `nodeId` покажет активную страницу с её id → `get_screenshot` на этот id.
> `get_metadata` по странице целиком падает (ответ больше 3 МБ), поэтому страница
> изучается скриншотом в высоком разрешении с последующим кропом.

## Сверка по макету «🔵 Главная» (страница «⬒ Desktop»)

Страница 25503×35983, светлая и тёмная темы рядом; кроме «Главной» на ней лежат
«Реферальная программа», «Доход» и «Воркеры».

### Где Segment Control, а где нет

Компонент применяется не везде, где есть выбор — в макетах это различается по экранам:

| Место | В макете |
|---|---|
| Главная → период графика (5 мин / 1 ч / 24 ч) | **Segment Control** — общий контейнер, активный сегмент белый |
| Воркеры → фильтр статусов (Все / Активные / …) | **Segment Control** со счётчиками и цветными точками |
| Воркеры → лента групп (Все / Москва / Псков / …) | **Segment Control Line** — текстовые вкладки со счётчиками на общей линии |
| Доход → период (7 дн / 30 дн / 90 дн) | отдельные пилюли с зазорами, **не** Segment Control |
| Калькулятор → период дохода | **Segment Control Line** на цветной подложке |

### Прочее с этой страницы

- Балансы монет показываются с **восемью знаками**: «0,39006783 BTC», «0,00012591 BTC».
- «Доход»: три карточки в ряд — первая целиком индиго с шевроном, две другие белые.
- В шапках денежных колонок стоят круглые значки монеты, доллара и рубля.
- «Воркеры»: карточки статусов справа несут **цветной круглый значок** с глифом
  (галочка / восклицательный знак / крестик / пауза), а не серый кружок с точкой.
- «Реферальная программа»: в таблице выплат есть колонка «Реквизиты» с замаскированным
  значением вида `WU/****9uG1` и карандашом, либо кнопкой «+ Добавить».

## Сверка по макету «🔵 Профиль [Web]» → «Сводка профиля»

Страница 39107×21983: светлая и тёмная темы, состояния «без аккаунта», «пустое»
и «заполненное», плюс флоу наблюдателей.

- **Вкладки раздела — Segment Control**: один светлый контейнер, активная вкладка белой
  пилюлей с тенью. Счётчики показываются и при нуле («Наблюдатели 0»).
- **В суб-аккаунтах три монеты**: BTC (TH/s), LTC (GH/s) и **ZEC (KSol/s)** — третьей
  монеты в прототипе не было вовсе.
- Плитка суб-аккаунта: имя + бейдж роли, общий баланс, три строки хэшрейта,
  строка «Воркеры» с четырьмя цветными счётчиками.
- **Наблюдатели на сводке — плитками**: имя + «+N», иконки монет, подпись, до трёх
  тегов доступа со свёрткой в «+N», срок действия (истёкший — красным),
  поле ссылки с копированием и кнопкой QR. У выключенной ссылки поля со ссылкой нет.
- Пустое состояние наблюдателей: иллюстрация, текст «У вас еще нет созданных ссылок
  наблюдателей» и кнопка «+ Создать».
- **Внизу сводки два промо-баннера** с крестиком: про порог продажи ЦВ и про
  партнёрскую программу.
- В карточке «Мой профиль» только две строки — «Последняя сессия» и
  «Google Authentication»; строки верификации там нет.
- Карточка «Последние уведомления»: красный счётчик у заголовка, список событий
  с описанием и датой, внизу «Прочитать все».

## Страница «Действия с телефоном» (`531:19524`)

Три флоу — добавление, смена и удаление номера, в светлой и тёмной темах.

- **Модалка «Личные данные»** (открывается карандашом в карточке «Мой профиль»):
  три поля в ряд — Фамилия, Имя, Отчество; поле «Дата рождения» с иконкой календаря;
  секция «Контакты» тремя строками — Телефон, Почта, Telegram: иконка слева,
  подпись и значение, справа круглая кнопка с карандашом. У подтверждённой почты
  зелёная точка после адреса, у телефона и Telegram значения нет.
- **Модалка «Добавить номер телефона»** — двухшаговая, с полоской прогресса из двух
  сегментов под заголовком:
  1. поле телефона с префиксом `+7` и подсказкой «Вы сможете авторизовываться
     по номеру телефона и паролю»;
  2. «Подтвердите действие», текст про код на email, поле «Код подтверждения»,
     ссылка «Вставить код» и таймер «Запросить новый код через 00:59».
- У модалок в макете есть крестик в шапке и одна основная кнопка «Продолжить».

## Компоненты дизайн-системы

Разбираются по страницам из `docs/figma-pages.md`.

### Button (`140:158`)

Шесть состояний: **Default** — indigo-400, **Hover** — indigo-500, **Active** — indigo-600,
**Loading** — светлая заливка со спиннером, **Disabled** — opacity .5,
**Focus (Tab)** — светлое кольцо indigo-alpha-048 вокруг формы.
Осветления фильтром и сдвига на пиксель, которые были в прототипе, в DS нет.

### Tab (`140:168`)

Пять состояний: Default (прозрачная), **Hover — появляется серая подложка**,
Active — подложка плотнее, Selected — белая заливка и тёмный текст, Focus — кольцо.
Счётчик может стоять как в строку с подписью, так и над ней.

### Input (`40001618:6205`)

Состояния: **Default** — серая заливка, **Hover** — плотнее,
**Typed/Focus — белое поле с серой рамкой** (а не тёмно-серое, как было в прототипе),
**Error** — розовая заливка и красное сообщение под полем,
**Error-Typed** — белое поле с красной рамкой. У заполненного поля справа кнопка
очистки «×», у многострочного — счётчик символов вида `0/100`.

### Modals / Base (`140:739`)

Шапка: заголовок, описание под ним и круглая кнопка «×» справа; в варианте с возвратом
слева круглая кнопка «‹». Внизу основная кнопка во всю ширину.

### Table cell (`40001644:465`)

Варианты ячеек: чекбокс, точка статуса + имя, иконка монеты + тикер + чип «+1»,
цветные теги статуса, чипы уровней со свёрткой в «+N», кнопки, ссылка со стрелкой
«Name →». Ячейка может иметь собственную цветную подложку (например красную
для проблемных строк).

### Нейтраль контролов

Checkbox, Radio, Switch и Select используют одну альфа-рампу от **`#6b7280`**, а не от
`#737373`, как было снято с прод-сборки. Поверхность контрола — `#6b72801f`,
наведение — `#6b728029`, обводка — `#6b728029`, усиленная обводка (Outline hover) —
`#6b728052`. В прототипе это токены `--ctrl`, `--ctrl-hover`, `--ctrl-line`, `--ctrl-line-h`.

Общая механика состояний у всех четырёх: **Active — `opacity .8`**, **Disabled — `opacity .4`**,
наведение на «включённый» контрол затемняет акцент до `#5369e0` (`--brand`).

### Checkbox (`193:4463`)

Бокс 18×18, radius 6, клик-зона 24×24. Оси: `State` (Default / Hover / Active / Disabled /
Focus (Tab) / Error), `Type` (Empty / Fill / Multiple), `Checked`.

| Состояние | Заливка | Обводка |
|---|---|---|
| Выключен | `#6b72801f` | 1px `#6b728029` |
| Выключен, наведение | `#6b728029` | 1px `#6b728029` |
| Включён | `#7086fc` | нет |
| Включён, наведение | `#5369e0` | нет |
| Ошибка | `#6b72801f` | 1px `#ef4444` |

Галка — белый вектор 10×7. «Multiple» (частичный выбор) — акцентная заливка и белая
полоска 8×2 с полным скруглением.

### Radio (`193:4518`)

Тот же бокс, но `radius 999`: выключен — `#6b72801f` + обводка, включён — `#7086fc`
с белой точкой 10×10, наведение на включённый — `#5369e0`. Состояния Error у радио нет.

### Switch (`193:4291`)

Дорожка 40×24, radius 999, внутренний отступ 2, ползунок — белый круг 20×20
(ход 16px). Выключен `#6b72801f`, наведение `#6b728029`; включён `#7086fc`,
наведение `#5369e0`.

### Select (`223:21631`)

Оси: `Size` (L 56 / M 48 / S 32), `Type` (Primary / Secondary / Outline),
`State` (Default / Hover / Active / Focus / Filled / Error / Error Filled / Disabled /
Disabled Fill), `Open`, плюс булевы `Label` и левая иконка.

Контейнер: высота по размеру, radius 16, padding `0 12`, gap 4.

| Тип | Заливка | Наведение |
|---|---|---|
| Primary | `#ffffff` | `#ffffffe0` |
| Secondary | `#6b72801f` | `#6b728029` |
| Outline | нет, обводка `#6b728029` | обводка `#6b728052` |

Плейсхолдер — 16 SemiBold `#9ca3af`. В состоянии Filled внутри контейнера две строки:
подпись 12 Medium `#6b7280` и значение 16 SemiBold `#111827`. Ошибка — обводка `#ef4444`.
Внешняя подпись над полем — 14/18 Medium `#9ca3af`, отступ 8.

**Dropdown-Menu (`40001620:3283`)**: radius 16, белая подложка, тень `0 4 24 #cbd0e966`
(это ровно `--shadow`), список с боковым отступом 12 и шагом 4 (размер S) или 8 (M),
нижний отступ 4–8. Сверху опциональный блок «Top» (поиск, 50px).

**Item (`40001586:6061`)**: высота 40, padding `8 0`, gap 8. Текст — 14/18 SemiBold (S)
или 18/22 SemiBold (M), цвет `#111827`. **Наведение красит текст в `#7086fc`, подложка
не появляется.** Выбранный пункт — тот же цвет плюс галка справа, а не заливка.
Тип Dangerous — `#ef4444`, Disabled — `opacity .4`.

### Chips (`40001996:331`)

Размеры M 48 / S 40, radius 16, padding 12, текст 16/20 SemiBold `#111827`,
заливка `#6b72801f`. Наведение заливку не меняет — добавляет обводку 1px `#6b728029`.
**Активный чип — сплошная нейтраль `#6b7280` с белым текстом, а не акцентная подложка.**
Disabled — заливка `#6b728014` и `opacity .4`. Есть опциональный крестик справа.

### Tags (`214:4856`)

Высота 32, radius 12, padding `0 12` (справа 4, если есть крестик), gap 2,
текст 14/18 SemiBold. Шесть типов, у каждого заливка — цвет с альфой, текст — тот же цвет:

| Тип | Заливка | Текст |
|---|---|---|
| Accent | `#7086fc1f` | `#7086fc` |
| Blue | `#3b82f629` | `#3b82f6` |
| Green | `#22c55e29` | `#22c55e` |
| Orange | `#f59e0b29` | `#f59e0b` |
| Red | `#ef444429` | `#ef4444` |
| Gray | `#6b728029` | `#111827` |

Наведение — `opacity .8`. Состояние Selected — сплошная заливка цветом и белый текст.

### Status (`214:4829`)

Отдельный от Tags компонент: высота 24, radius 8, padding `0 8`, gap 4,
текст **12/16 Bold**, слева точка 8×8 (опционально иконка 16). Четыре типа:

| Тип | Заливка | Текст | Точка |
|---|---|---|---|
| Success | `#22c55e29` | `#22c55e` | `#22c55e` |
| Error | `#ef444429` | `#ef4444` | `#ef4444` |
| Warning | `#f59e0b29` | `#f59e0b` | `#f59e0b` |
| Offline | `#6b72801f` | `#9ca3af` | `#6b7280` |

Наведение — `opacity .8`.

### Alert (`40001751:1894`)

Radius 16, padding `12 16`, gap 8. Слева иконка 16 со сдвигом вниз на 2,
справа колонка с шагом 4: заголовок 18/22 SemiBold и описание 16/20 Medium.
Заголовок и описание опциональны. Типы Success / Warning / Error / Info
(`#22c55e`, `#f59e0b`, `#ef4444`, `#3b82f6`) в двух состояниях: **Alpha** — заливка
цветом с альфой 29 и текст того же цвета, **Fill** — сплошная заливка и белый текст.
Обводки у алерта нет.

### Counter (`193:4900`)

Два размера: **M — круг 20×20** с числом 12/16 SemiBold белым, **S — точка 8×8** без текста.
Скругление полное. Шесть типов: Primary `#7086fc`, Neutral `#6b7280`, Positive `#22c55e`,
Informative `#3b82f6`, Warning `#f59e0b`, Negative `#ef4444`.

### Notification (`40001812:5858`)

На странице один компонент без вариантов: карточка radius 16, padding `12 16`, gap 8,
подложка `#6b728014`. Слева колонка с шагом 4 — заголовок 14/18 SemiBold, текст 14/18
Medium `#6b7280` и опциональная кнопка-ссылка 14 SemiBold `#7086fc`; справа иконка 24.

### Tooltip (`40001512:4127`)

Четыре варианта по направлению: Up / Bottom / Left / Right. Пузырь — radius 12,
padding 12, подложка **`#9ca3af`** (нейтраль 400, не тёмная), текст 14/18 Medium белый,
хвостик-треугольник 17×6 того же цвета.

### Pagination (`40001674:845`)

Собирается из четырёх кусков: `Pagination List Desc` (дорожка), `Pagination Item`,
`Pagination-Prev` / `Pagination-Next` и `Pagination Gap` (многоточие).
Отдельно есть `Dots Pagination` — точечная, для мобильных каруселей.

Дорожка: radius 16, padding 4, gap 4, подложка `#6b72801f`.
Элемент: **40×40** (размер S — 36), radius 12, текст 14/18 SemiBold.

| Состояние | Заливка | Текст |
|---|---|---|
| Обычный | нет | `#9ca3af` |
| Наведение | `#6b728029` | `#9ca3af` |
| Выбранный | `#ffffff` | `#111827` |

Стрелки: 40×40, radius 12, padding 8, иконка 24 цветом `#111827`,
в disabled — `#6b7280`. Многоточие — 16/20 SemiBold `#9ca3af`.

## Что из этого перенесено в прототип

- Gilroy (5 весов, woff2 прямо с прода) — в `public/fonts`, подключён из `src/styles/app.css`
  (в архивной однофайловой версии был вшит в `index.html` через base64).
- Полный набор токенов и тёмная тема.
- Настоящий логотип и favicon.
- Экраны `/login`, `/register`, `/restore` и шаг ввода кода.
- Все внешние адреса: документы, база знаний, бот алертов, реферальная ссылка, ссылка наблюдателя.

## Что перенести нельзя

Внутренние экраны прода закрыты авторизацией — сверка по ним идёт только по макетам Figma.
Имена иконок в сборке вырезаны минификатором, поэтому набор иконок собирается из дизайн-системы,
а не из прод-бандла.
