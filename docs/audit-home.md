# Аудит «Главной»: что не совпадает с макетом

Сверка прототипа с файлом 🔵 **Главная [Web]** — `CZTB9Ck7pnF4goeC8xLt8e`,
страница «⬒ Desktop» (`57:84528`). Эталон раскладки — кадр `185:105205`
(секция «Смена монеты», BTC), состояния — остальные секции.

Замеры прототипа сняты из DOM при ширине окна 1920 (контент 1641),
макета — через `use_figma` по `absoluteBoundingBox` и стилям текста.
Дата сверки: 12.09.2026.

Продолжение — [audit-tokens.md](audit-tokens.md): палитра и разделы профиля.

## A. Поведение и состав экрана

### A1. Наблюдатель видит не то, что в макете

В макете (секция «Наблюдатель» `1099:95409`) Главная **собирается из разрешений**
вотчера, и в ней нет ни рефералки, ни адресов майнинга, ни контактов, ни промо.
Состав кадров:

| Кадр | Блоки | Сайдбар |
|---|---|---|
| `189:154154` | три карточки, график, таблица | Главная / Воркеры / Финансы |
| `189:156127` | три карточки, таблица | Главная / Финансы |
| `189:162153` | три карточки, график | Главная / Воркеры |
| `189:160463` | только таблица | Главная / Финансы |

Правило из макета дословно: **«В вотчере есть Главная страница, если настроены
на просмотр все или хотя бы одна из страниц: Воркеры, Доход, Выплаты, Мои активы.
Во всех остальных случаях — Главной страницы нет.»**

В прототипе при `role=observer` рендерится **полный экран владельца** плюс баннер
«👁 Режим наблюдателя», которого в макете нет вообще; сайдбар остаётся полным
(все 8 разделов). Ось «разрешения вотчера» в сценариях отсутствует.

### A2. Мёртвые клики

На Главной 4 интерактивных элемента ни на что не влияют:

| Элемент | Что должно быть по макету |
|---|---|
| «Продать» в карточке «Доход» | «Ведет в Мои Активы» |
| Стрелка в карточке «Доход» | «Ведет в Доход» |
| Стрелка в карточке «Средний хэшрейт» | связь не размечена |
| Кнопка скачивания в «Доход/Выплаты» | выгрузка |

Связи размечены в макете подписями «Ведет в Доход» (4 шт.) и «Ведет в Мои Активы»
(2 шт.), а в секции «Переключени на другие разделы» (`1447:159876`) коннекторы
с Главной ведут на «Promminer: реферальная программа» (`1586:110905`),
«Доход» (`1448:167339`) и «Воркеры» (`1469:179451`, `1469:182107`).
Плитки воркеров в прототипе уже ведут в «Воркеры» с фильтром — это совпадает.

### A3. Нет монеты ZEC

В макете три состояния переключателя: BTC (`185:105205`), LTC + DOGE (`185:108306`)
и **ZEC** (`1954:74747`). В прототипе ось `coin` знает только `btc` и `ltc`.

### A4. Счётчики воркеров не зависят от монеты

Макет для LTC + DOGE: «Воркеры (212)» — 23 / 68 / 21 / 100.
Прототип при `coin=ltc` показывает те же 5 979 — 1 398 / 1 230 / 851 / 2 500,
что и при BTC: числа привязаны к оси `health`, а не к монете.
Хэшрейт при этом монетозависимый и совпадает (74 243 GH/s).

## B. Раскладка

| # | Что | Макет | Прототип |
|---|---|---|---|
| B1 | Шаг между блоками экрана и между карточками в ряду | 16 | 12 |
| B2 | Высота ряда из трёх карточек | 288 | 242 |
| B3 | Высота карточки графика | 611 (тело 449) | 470 (тело 330) |
| B4 | Карточка «Доход/Выплаты» | табличная: паддинг 8/8/16/8, шапка 80 | обычная: паддинг 24, шапка 48 |
| B5 | Шапка таблицы | 48 | 60 |
| B6 | Панель «Общие данные по всем монетам» | `#7086fc` 12 %, радиус 16, паддинг 12/24 | `--accent-ghost` (`#6366f1` 8 %), `--r-s`, паддинг 14/16 |

### B7. Реферальный блок сложен иначе

В макете (`185:105544`) карточка — **две колонки по 794** с зазором 48:

- слева медаль 152, справа от неё строка «Ваша комиссия / Хэшрейт рефералов /
  монета», под ней подпись «До следующего уровня: 59 800 ТH/s» и дорожка **590**;
- справа отдельная панель «Общие данные по всем монетам».

В прототипе всё выстроено в один ряд, а дорожка растянута на всю ширину карточки
(1593 вместо 590). Строки «До следующего уровня» нет совсем.

## C. Типографика

Подписи и значения в прототипе на шаг-два мельче макета.

| # | Элемент | Макет | Прототип |
|---|---|---|---|
| C1 | Крупное значение в карточке («0,39006783 BTC») | 32 SemiBold | 24 SemiBold |
| C2 | Подпись «Текущий баланс», «За 24 ч» | 14 SemiBold | 12 SemiBold |
| C3 | Значения «За 24 ч» / «За все время» | 18 SemiBold | 16 SemiBold |
| C4 | Подписи под ними | 14 Medium | 12 SemiBold |
| C5 | «Продать» | 16 SemiBold | 14 SemiBold |
| C6 | «Общие данные по всем монетам» | 24 SemiBold | 16 SemiBold |
| C7 | Значение «Ваша комиссия» («5% (Бронза)») | 24 SemiBold | 16 SemiBold |
| C8 | «Подробно о программе» | 16 SemiBold | 14 SemiBold |
| C9 | Монета в строке над карточками («BTC») | 16 SemiBold | 14 SemiBold |
| C10 | Курсы («BTC: 80 892 $ • …») | 14 Medium | 12 SemiBold |
| C11 | Заголовки колонок таблицы | 14 SemiBold | 12 Medium |
| C12 | Ячейки таблицы | 14 SemiBold | 14 Medium |

Совпадают: заголовки карточек 24 SemiBold, заголовок графика (392×30),
сегмент-контрол диапазона (198×48 против 200×48), строки таблицы 48.

## D. Размеры контролов

| # | Контрол | Макет | Прототип |
|---|---|---|---|
| D1 | Кнопки зума на графике | 48×48 | 40×40 |
| D2 | Кнопка скачивания в «Доход/Выплаты» | 48×48 | 40×40 |
| D3 | Выбор диапазона дат | 235×48, текст 16 | 213×48, текст 14 |
| D4 | Кнопка «Подключить воркер(а)» | 212×48 | 221×48 |

## E. Тексты и форматирование

| # | Макет | Прототип |
|---|---|---|
| E1 | «Подключить воркер» | «Подключить воркера» |
| E2 | При LTC + DOGE подпись героя — «Средний хэшрейт за 24 ч» | всегда «За 24 ч» |
| E3 | Подпись «Текущий баланс» одна на две монеты | печатается для каждой монеты |
| E4 | «5,4098 LTC» | «5,40980000 LTC» (жёстко 8 знаков) |
| E5 | 25 % уровня рефералки — «VIP» | «Алмаз» |

## F. Замечено рядом, вне Главной

- «Вид на 1440» — в макете отдельные кадры (например `193:38835`, `187:136893`),
  в прототипе адаптива под 1440 нет.
- На «Рефералах» в карточке «Реферальная ссылка» (`1586:110912`) есть иллюстрация
  `Money dollar_2`, а в прототипе там остался мёртвый `<div class="orb">`
  (стилей у `.orb` в `app.css` нет — правило не доехало из `archive/index.html`).
