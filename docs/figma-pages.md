# Индекс страниц Figma

Получен через `use_figma` (Plugin API) одним запросом:

```js
return figma.root.children.map((p,i)=>({i, id:p.id, name:p.name}));
```

Это единственный надёжный способ: `get_metadata` без `nodeId` возвращает только первый
элемент верхнего уровня, а `loadAllPagesAsync` в `use_figma` запрещён (и не нужен —
`figma.root.children` отдаёт все страницы сразу).

## 🔵 Профиль [Web] — `QurMA1NnX8rhAOYDsaxedC`

Актуальные макеты — всё до «Уведомления» включительно; ниже архив и черновики.

| Страница | node-id | Статус |
|---|---|---|
| Сводка профиля | `451:6014` | разобрана |
| Действия с телефоном | `531:19524` | разобрана |
| Действия с почтой | `531:19523` | разобрана |
| Действия с телеграмом | `531:19525` | разобрана |
| Флоу подтверждения | `493:9597` | |
| Верификация и реквизиты | `759:68419` | |
| Центр суб-аккаунтов | `840:27406` | |
| Наблюдатели | `882:61237` | |
| Безопасность | `588:83412` | разобрана |
| Уведомления | `940:22834` | |

### Модалки «Сводки профиля»

Все 550 в ширину, Modal Base = шапка 80 + тело (отступы 0/24, шаг 20) + футер 24
с кнопками 56/r16. У части шапка с иллюстрацией 542×144 вместо заголовка.

| Модалка | node-id | В прототипе |
|---|---|---|
| Информация об аккаунте | `1490:80238` | `subinfo` |
| Изменить наблюдателя | `2219:32311` | `obsedit` |
| Удалить ссылку наблюдателя? | `1444:163287` | `obsdel` |
| Создать ссылку наблюдателя | `2219:32420` | `observer` |
| QR-код наблюдателя | `2219:32518` | `qr` |
| Добавить суб-аккаунт | `880:41731` | `subacct` |
| Личные данные | `529:18207` | `personal` |
| Настройки | `2219:33334` | `settings` |
| Карточка уведомления | `531:69489` | `noteinfo` |
| Выход из аккаунта | `1037:55175` | `logout` |

Иллюстрации шапок выгружены в `public/modal-*.png` через `download_assets`.

### Контакты (телефон, почта, Telegram)

Телефон и почта устроены одинаково. Число шагов читается не на глаз, а из
варианта компонента `progress` (`Selected Step` / `Amout Step`):

- **добавление и изменение — 4 шага**: подтверждение по второму каналу →
  значение → код на новый канал → экран успеха с кнопкой «Отлично»;
- **отвязка — 3 шага**: красное подтверждение **на 500** (а не 550), затем
  подтверждение по второму каналу → код → успех.

Код всегда уходит по «другому» каналу: смена почты подтверждается телефоном
и наоборот. Кнопка «Вставить код» есть только там, где код пришёл на email.
У Telegram шагов нет — бот, поле и ошибки, шапка с `modal-tg.png`.

### Уведомления

Страница макетов — `1022:56912`, компонент пагинации — `1026:104278`.
Оболочка та же, что у таблиц: карточка с паддингом 8, шапка 80 с паддингом 16,
пагинация с паддингом **4/16/16/16** (то есть 24 от края карточки).

| Экран | node-id |
|---|---|
| Нет уведомлений | `1024:102866` |
| Есть уведомления | `1022:57336` |
| Сортировка | `1026:103653` |

Список — таблица со строками 48 (60, если описание в две строки) и колонками
точка 24 / заголовок 400 / описание / дата 130, ячейки с паддингом 12
и разделителем снизу. Непрочитанное: точка 12 акцентом, заголовок SemiBold,
текст и дата Primary. Прочитанное: точки нет, заголовок Medium, текст и дата
Secondary.

В шапке — ссылка «Прочитать все» с иконкой `Essentional, UI / Check`,
селект фильтра 48 и кнопка «Настройка» 48 с иконкой
`Settings, Fine Tuning / Tuning`.

### Наблюдатели

Страница макетов — `1346:48604` («обновление по созданию 17.04.26»).
Карточка такая же, как у суб-аккаунтов: паддинг 8 и шапка 80 внутри.

| Экран | node-id |
|---|---|
| Нет наблюдателей | `1346:48605` |
| Есть наблюдатели | `1346:48833` |
| Тултип на «+N» | `1346:49489` |
| Массовое удаление | `1346:49380` |

| Модалка | node-id | В прототипе |
|---|---|---|
| Создать ссылку наблюдателя | `1393:165559` | `observer` |

Таблица: одна шапка 48 на фоне `#f3f4f6` с радиусом 16, строки 48,
ячейки с паддингом 12, подписи 14/18 Tertiary. Колонки — чекбокс,
Аккаунт, Изменен, URL-адрес, Описание, Монеты, Разрешения, Срок действия
и три ячейки действий. Скрытые значения свёрнуты в серый чип «+N»
с тултипом. Срок у истекшей ссылки красный, и у такой строки нет
кнопок копирования и QR — остаётся только меню.

Выбор строк показывает над таблицей полосу 72 с радиусом 16 на фоне `--bg`:
«Выбрано элементов: N», ссылка «Очистить выбор» и красная кнопка «Удалить».

Кнопки действий в строке — круг 36 с обводкой `--border` и иконкой **16**,
а не голая иконка. Между строками 1px разделитель `--border`, он есть и под
последней строкой.

Модалка создания обновилась: прогресс на **три** шага, у основного аккаунта
бейдж «Основной», монеты списком из трёх строк (LTC и DOGE одной),
кнопка подтверждения называется «Подтвердить».

В пустом экране макета шапка подписана «Наименование», а в заполненном —
«Аккаунт»; в прототипе везде «Аккаунт».

### Центр суб-аккаунтов

Страница макетов — `1482:83256`. Карточка с паддингом **8**, внутри шапка 80
с паддингом 16 и таблица без внешних отступов.

| Экран | node-id |
|---|---|
| Нет имени аккаунта | `1482:83305` |
| Нет суб-аккаунтов | `1482:92761` |
| Есть активные суб-аккаунты | `1482:93522` |
| Показаны архивные | `1484:94425` |
| Активация (тост) | `1484:98684` |

| Модалка | node-id | В прототипе |
|---|---|---|
| Добавить суб-аккаунт | `1482:83893` | `subacct` |
| Имя занято | `1482:83938` | ось `saerr=busy` |
| Проверка уникальности | `1482:83957` | ось `saerr=load` |
| Суб-аккаунт создан | `1482:83968` | `subacct`, шаг 2 |
| Архивировать суб-аккаунт | `1482:83662` | `subarch` |

Pagination дизайн-системы: подпись «Показать» стоит **над** селектом
(селект 110×48, радиус 16, текст 14/18), справа от него «Показано N–M из K»
по центру коробки селекта, а блок страниц — 136×48 с элементами 40 и радиусом 12.

Таблица: две шапки — группы 32 и колонки 48, обе с чередующимися полосами
(`#f5f5f5` у «Аккаунт/Баланс» и «Хэшрейт», `#e5e5e5` у «Воркеры» и «Доход»),
подписи 14/18 Tertiary **слева**, строки 74 с паддингом ячеек 12, значения
14/18 Primary тоже слева. Итог «Все аккаунты» — отдельная полоса с радиусом 16
и фоном `#7086fc1f`. Бейджи 24/r8: «Основной» залит акцентом, «Суб-аккаунт» —
Secondary. У строк суб-аккаунтов справа кнопка с `Security / Eye` Bold Duotone.

Тогл «Показать аккаунты в архиве» **добавляет** строки поверх лимита, счётчик
на вкладке при этом не меняется — считаются только неархивные.

Активация архивного суб-аккаунта идёт без подтверждения, сразу тостом —
модалки для неё в макете нет. В пустом экране макета опечатка
(«имя акканта»), в прототипе написано «имя аккаунта».

### Верификация и реквизиты

Страница макетов — `759:68419`, на ней две секции с одинаковой раскладкой:
`778:17706` — **светлая** (с неё сняты цвета), `2219:146235` — тёмная и более
полная (в ней добавлен выбор банка). Контент 1652: левая колонка 460,
правая 1176, между ними 16.

| Экран | node-id |
|---|---|
| ИП, не заполнено | `2219:146409` |
| ИП, заполнено | `2219:147310` |
| Юрлицо, не заполнено | `2219:146318` |
| Физлицо, не заполнено | `2219:146236` |
| Ошибка «Код налоговой» | `2219:146882` |
| Ошибка «ИНН», 12 цифр | `2219:146974` |
| Ошибка «ИНН» у юрлица, 10 цифр | `2219:147107` |
| Ошибка «Размер файла» | `2219:147594` |

| Модалка | node-id | В прототипе |
|---|---|---|
| Добавить выписку | `2219:147552` | `vdocm` |
| Добавить счет | `2219:147444` | `vaccm` |
| Добавить счет с БИК (светлая) | `2263:11089` | `vaccm` |
| Выбор банка | `2219:147513` | попоувер `vbank` |
| Счет успешно добавлен | `2219:147538` | `vaccm`, шаг 2 |
| Банк не выбран | `2219:147467` | ось `verr=bank` |

Правовая форма — компонент **Chips** (48, r16, паддинг 12), поставленные
вертикально: активный залит `#6b7280` с белым текстом, остальные `--ctrl`.
Это исключающий выбор, но Segment Control вертикальным в системе не бывает,
поэтому взято то, что в макете.

У физлица показывается только карточка «Верификация» — выписки, счета
и помощи с открытием счета у него нет.

Состояния переключаются осями `verif` (данные), `vdoc` (выписка),
`vacc` (расчетный счет) и `verr` (ошибка в поле). У ИНН текст ошибки
зависит от правовой формы: 12 цифр у ИП и физлица, 10 у юрлица.

Заголовки памятки («Как физическое лицо вы можете:») — Primary, а не синие:
у текстового слоя две заливки, сверху `#111827`, под ней
`Content/Sentiment/Informative`. Читать нужно последнюю в массиве `fills`.

Поле ввода дизайн-системы (`.inp.fl`, помощник `inpField`) ведёт себя так:
пока пусто — подпись работает плейсхолдером по центру (16/20 SemiBold,
Secondary), как только в поле появился текст — уезжает наверх подписью 12
и показывается крестик очистки. Переключение на `:has(input:placeholder-shown)`,
без скриптов, поэтому работает прямо во время набора.

### Безопасность

Экран — две карточки слева (2FA и «Безопасность данных») и «Сессии» справа,
строка секции: плашка 56, заголовок 16/20, подпись 14/18 и действие справа.

Отдельные узлы экрана:

| Что | node-id |
|---|---|
| «Завершить все сессии, кроме текущей» | `990:50617` |
| Строка «Удаление аккаунта» после заявки | `1008:52633` |

«Завершить все сессии» — не текстовая ссылка, а Button `Gost Secondary`
размера `XS (28)`: во всю ширину (412), радиус 12, текст по центру, 16/20
SemiBold красным. Состояние после удаления — та же строка секции с бейджем
`Badge / Secondary` «В процессе» (24, r8, паддинг 4/8, фон `#7086fc1f`,
обводка `#7086fc7a`) и акцентной ссылкой «поддержку» в подписи; кнопки
отмены и алерта в макете нет.

| Модалка | node-id | В прототипе |
|---|---|---|
| Подключите Google Authenticator | `971:103549` | `fa2on` |
| Выключение 2FA-защиты | `971:104468` | `fa2off` |
| Изменение пароля | `972:36256` | `pwd` |
| Все сессии | `1008:48445` | `sessions` |
| Завершить эту сессию? | `1008:50987` | `sessend` |
| Завершить все сессии? | `1008:49881` | `sessall` |
| Удаление аккаунта | `1008:52491` | `acctdel` |

Состояние 2FA — ось сценария `fa`. У модалок 2FA тело выше обычного: Modal
Content там 506, а не 348.

Состояния ошибок поля («Поле обязательно», «Занято другим пользователем»,
«Некорректный формат», у Telegram «Аккаунт не привязан к боту») переключаются
осью сценария `cerr`.

| Модалка | node-id | В прототипе |
|---|---|---|
| Добавить номер телефона | `531:85091` | `phoneadd` |
| Изменить номер телефона | `534:61269` | `phoneedit` |
| Отвязать номер телефона? | `534:64418` | `phoneunlink` |
| Отвязать номер телефона (код) | `579:25282` | `phonecode` |
| Добавить почту | `582:23978` | `mailadd` |
| Изменить почту | `582:24360` | `mailedit` |
| Отвязать Email? | `582:24685` | `mailunlink` |
| Отвязать почту (код) | `582:24692` | `mailcode` |
| Привязка Telegram | `588:49263` | `tgadd` |
| Изменение Telegram | `588:50121` | `tgedit` |
| Отвязать Telegram? | `588:24880` | `tgunlink` |

Состояние привязки — оси сценария `phone`, `mail`, `tg`.

Архив (не смотрим): `1857:87153` Доработки профиля, `788:55149` Workspace 2.0,
`1031:106451` про воркера, `146:14224` Preview, `1:4` Workspace 1.0, `305:26484` References.

## 💙 Оболочка — `6qgYjD01F77zgS3blrbP7F`

Отдельный файл с поведением каркаса. Страница «⬒ Макеты в разработку»,
секция «Оболочка - 1920» (`54:20057`):

| Раздел | node-id | Статус |
|---|---|---|
| Макеты: Свернутый сайдбар | `54:20058` | разобран |
| Flow: Свернутый сайдбар | `56:10365` | |
| Макеты: Кнопка баланса | `68:25450` | разобран |
| Макеты: Уведомления | `66:14709` | разобран |
| Flow: Первый вход — добавление username | `54:20561` | |
| Макеты: Первый вход — добавление username | `56:10995` | |
| Flow: Суб-аккаунты | `56:11787` | |
| Макеты: Суб-аккаунты | `56:11846` | |

Разобранное: баланс ведёт в «Мои активы»; в свёрнутом сайдбаре кнопка
«Свернуть» превращается в стрелку вправо без подписи, логотип — в один знак;
шапка попоувера уведомлений — заголовок 20 SemiBold, красный счётчик рядом
(от 100 показывается как 99+) и акцентная ссылка «Прочитать все».

## 💙 Дизайн-система — `8rrL0jaovYD3jsrq7WF1L1`

Токены: `152:54175` Brand Colors, `152:54455` Global Tokens.

Базовые компоненты:

| Компонент | node-id | | Компонент | node-id |
|---|---|---|---|---|
| Alert | `40001751:86` | | Pagination | `40001674:845` |
| Button | `140:158` | | Radio | `140:165` |
| Checkbox | `140:164` | | Scroll | `40001511:15373` |
| Chips | `40001996:193` | | Select | `140:162` |
| Counter | `193:4842` | | Search | `40001612:14824` |
| Color picker | `40003359:4198` | | Status | `214:4821` |
| Date Picker | `214:2522` | | Switch | `140:163` |
| Input | `40001618:6205` | | Tab | `140:168` |
| Indicator | `40003231:867` | | Tags | `214:4822` |
| Notification | `40001812:5858` | | Tooltip | `40001512:4126` |

**Все базовые компоненты разобраны и перенесены** — спецификации в `docs/prod-analysis.md`:
Button, Tab, Input, Modals/Base, Table cell, Checkbox, Radio, Switch, Select,
Chips, Tags, Status, Alert, Notification, Tooltip, Pagination, Counter,
Date Picker, Header, Footer, Sidebar.

**UI icons** (`140:124`) — 1263 компонент-сета вида `UI icons / Группа / Имя`,
у каждого варианты по стилю. 45 иконок прототипа выгружены отсюда (Bold Duotone),
плюс Telegram со страницы **Brands** (`140:127`). Подробности — в `docs/icons.md`.
Остальные страницы иконок: `140:125` Flags, `140:126` Payments, `140:123` IconBox.

Не смотрели: Scroll, Search, Indicator, Color picker, Tabbar (мобильный),
Logo, Loader, Skeleton и составные страницы
(Workers, Finance, My referrals, Profile, Sub-accounts).

Остальное: `140:739` Modals/Base, `40001644:465` Table cell,
`140:141` Header, `140:142` Footer, `140:143` Sidebar, `140:144` Tabbar,
`140:147` Workers, `140:148` Finance, `140:152` My referrals, `140:154` Profile,
`40001465:1909` Sub-accounts, `140:138` Logo, `140:123` IconBox, `140:124` UI icons,
`140:130` Loader, `140:131` Skeleton.
