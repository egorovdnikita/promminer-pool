# Аудит: «Сводка по аккаунтам» и «Мониторинг»

Источники: 🔵 **Сводка по аккаунтам** — `V8iE8aEZMTVjTmB0JFOvvd`, страница
«Desktop» `90:23278`; и страница `/monitoring` на проде, снятая под
авторизацией 14.09.2026.

## Главное: «Мониторинг» и «Сводка» — это два разных раздела

В прототипе пункт сайдбара «Мониторинг» открывал **сводку по всем
аккаунтам** (заголовок вкладки так и назывался — «Главная сводки»).
На проде всё иначе:

| Раздел | Что это на самом деле |
|---|---|
| «Мониторинг» (`/monitoring`) | промо-страница раздела: «Мониторинг оборудования», кнопка «Получить предложение», списки параметров и три шага подключения |
| «Сводка по аккаунтам» | отдельный режим, в который попадают **из переключателя аккаунтов** в шапке, а не из сайдбара |

Поэтому экран разделён: `monitor` — промо с прода, `summary` — сводка
из макетов. В шапке «Сводка по аккаунтам (46)» показывается теперь
на `summary`, а карточка сводки в попоувере аккаунта стала кнопкой,
которая туда ведёт.

## 1. Мониторинг (прод)

Три карточки сверху вниз:

1. **Герой** по центру: «Мониторинг оборудования» 32, описание 16,
   кнопка «Получить предложение», под ней иллюстрация (телефон на фоне
   карты мира) шириной около 880.
2. **Списки через разделители**: «Параметры для отслеживания
   оборудования:» (5 пунктов), «Возможности:» (4 пункта) и ещё раз
   «Параметры для отслеживания оборудования:» (3 пункта про триггеры).
   Пункты — тире плюс текст 14.
3. **Три шага**: «Параметры для отслеживания оборудования:» и три серые
   плитки с номерами 1 / 2 / 3 в акцентном квадрате.

> На проде второй и третий заголовок совпадают с первым — судя по всему,
> опечатка в контенте: третий блок про триггеры и уведомления.
> Перенёс как есть.

## 2. Главная сводки (`92:23145`)

Структура в прототипе уже совпадала с макетом: ряд действий с селектором
монеты, три карточки, график общего хэшрейта, «Детализация хэшрейта»
со сгруппированной шапкой и «Общий доход» со ссылкой «Весь доход».

Размеры сверены:

| Элемент | Макет | Прототип |
|---|---|---|
| Карточка | 540×280, радиус 24, паддинг 24 | 539×**300** |
| Акцентная плашка внутри | 492×128 | 491×**142** |
| Плитка воркеров | 240×88 | 240×**92** |
| Баланс / хэшрейт | 32 | 32 |
| «За 24 ч», «За 5 мин» | 18 | 18 |
| Значение плитки | 24 | 24 |

Шрифты и состав совпадают, карточки выше макета на 20 — из-за высоты
акцентной плашки, она общая с Главной.

Колонки «Общего дохода» в макете: Дата · Количество аккаунтов ·
Хэшрейт, 24 ч · Доход с 1 TH, BTC · Доход, BTC · Доход, $ · Доход, ₽ —
совпадают с прототипом.

## Сделано

| # | Правка |
|---|---|
| 1 | «Мониторинг» стал промо-страницей с прода: герой, три списка через разделители, три шага |
| 2 | Сводка переехала на свой маршрут `summary` с заголовком «Сводка по аккаунтам» |
| 3 | Карточка «Сводка по аккаунтам» в попоувере аккаунта ведёт на этот экран |
| 4 | Селектор монеты в шапке переехал с «Мониторинга» на «Сводку» |

## Не разобрано

- Иллюстрация героя «Мониторинга» — на проде это не `<img>`, забрать
  файл не удалось; вместо неё мягкая акцентная подложка того же размера.
- Остальные экраны режима сводки: «Мои активы сводка» (`92:24121`),
  «Начисления сводка» (`92:23658`), «Воркеры сводка» (`101:30358`),
  «Детализация начислений» (`149:79204`) — в прототипе их нет.
- Модалки экспорта сводки (`310:91107`, `310:91022`) и пустое состояние
  (`95:42712`).
- Мобильная страница `90:23280`.
