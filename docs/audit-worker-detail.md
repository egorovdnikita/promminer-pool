# Аудит деталки воркера: что не совпадает

Сверка с файлом 🔵 **Воркеры [Web]** — `8mvRUmxWj1tT96Sb7Ou13h`, страница
«Десктоп» `223:98541`, эталон — кадр «Есть данные» `223:98556`.
Замеры прототипа сняты из DOM при ширине 1920. Дата сверки: 13.09.2026.

Разбор сверху вниз. Геометрия блоков сошлась вся: шапка 64, карточки
540×208, график 1232×705, колонка 404, события 529, баннер 160 на y953.
Расхождения — в наполнении: не хватает акцентной плашки, таймлайна
событий, нескольких контролов и иконок, плюс роли токенов у подписей.

## Оговорка про цвет

Страница нарисована в режиме **Gray** (`#111827` / `#6b7280` / `#9ca3af`),
а прототип реализует **Neutral** — это известное расхождение из
[audit-tokens.md](audit-tokens.md), общее для всех макетов, и в правки
по этому экрану оно не входит. Ниже сравниваются **роли** токенов:
Primary / Secondary / Tertiary, а не конкретные шестнадцатеричные коды.

## 1. Шапка

| # | Что | Макет | Прототип |
|---|---|---|---|
| 1.1 | Кнопка «назад» | **48×48**, радиус **16**, фон `--ctrl` | 40×40, радиус 12, без фона |
| 1.2 | Значок перед именем | Icon Box **24** слева от имени | значка нет |
| 1.3 | Статус | точка **8** цвета статуса + текст 16/20 SemiBold **Primary** | чип 78×28 с текстом 14 цветом статуса |
| 1.4 | Блоки связей | «Button Details» 248 и 311×48, **белая карточка**, радиус 16 | 236×48, прозрачные, фон только по ховеру |
| 1.5 | Подписи «Группы:», «Теги:», «Порог уведомлений:» | 14/18 Medium **Tertiary** | 14/18 Medium **Secondary** |
| 1.6 | Иконка действия в блоке связей | **24×24** справа внутри каждого блока | иконки нет |
| 1.7 | Чипы тегов | высота **32**, радиус **12**, фон — **цвет тега** 12–16 % | обычный серый `.tag`, высота 28 |

Имя 24/30 SemiBold и сам набор блоков совпадают.

## 2. Карточка сводки хэшрейта

| # | Что | Макет | Прототип |
|---|---|---|---|
| 2.1 | Верх карточки | **акцентная плашка 492×100**, радиус 16, `#7086fc`, текст белый | плашки нет, текст на белом фоне карточки |
| 2.2 | Подпись «Средний хэшрейт за 24 ч» | 14/18 SemiBold **белым** | 14/18 SemiBold Secondary |
| 2.3 | Значение 235,76 | 32/38 SemiBold **белым** | 32/38 SemiBold Primary |
| 2.4 | Зазор от плашки до строки | **12** | 16 |

Это самое заметное расхождение на экране: в макете сводка выглядит как
акцентный блок внутри белой карточки — ровно тот же приём, что в карточке
хэшрейта на «Воркерах». «За 5 мин» и «За 1 ч» (14/18 Medium + 18/22
SemiBold) совпадают.

## 3. Карточки «Показатели» и «Параметры»

| # | Что | Макет | Прототип |
|---|---|---|---|
| 3.1 | Заголовок карточки | 20/24 Medium **Secondary** | 20/24 Medium **Primary** |
| 3.2 | Шаг строк | **36** (строка 24 + зазор 12) | 40 (зазор 16) |
| 3.3 | «Цифровая валюта» | значок монеты **24** перед значением | значка нет |
| 3.4 | «Завод. номер» | кнопка **копирования 24** справа от значения | кнопки нет |

Прижатие строк к низу карточки, подписи 14/18 Medium Secondary и значения
20/24 SemiBold Primary совпадают. Иконка правки в шапке «Параметров» — 24,
на месте.

Флоу «Копирование номера, алерт» (`403:36099`) без кнопки из 3.4
недоступен целиком.

## 4. График

| # | Что | Макет | Прототип |
|---|---|---|---|
| 4.1 | Кнопки масштаба | **две 48×48**, фон `--ctrl`, радиус 16 | кнопок нет |
| 4.2 | Календарь | 256×48 | 236×48 |

Заголовок 24 SemiBold, сегмент 200×48, две оси, легенда и высота карточки
705 совпадают. В макете рядом лежат скрытые варианты фильтра (Select 110
и Chips 151) — это альтернативы сегмента, в видимом состоянии их нет.

## 5. События

Всё про события собрано здесь — и десктоп, и мобилка.

### 5.1 Десктоп: что не совпадало

| # | Что | Макет | Прототип | Статус |
|---|---|---|---|---|
| 5.1 | Строка события | высота **56**, без зазора | 36 + зазор 12 | исправлено |
| 5.2 | Маркер | **таймлайн**: кружок 28 цвета статуса 16 %, внутри точка 10, вниз линия 1×28 `#6b7280` 8 % | одна точка 8, линии нет | исправлено |
| 5.3 | Время | 12/16 Medium **Tertiary** | 12/16 Medium Secondary | исправлено |
| 5.4 | Заголовок «События» | 20/24 SemiBold | 24/30 — общее правило `.card h2` перебивало `.wdh2` | исправлено |

Название события 16/20 SemiBold, шапка 72 и карточка 404×529 совпадали
с самого начала.

### 5.2 Десктоп: прокрутка — совпадает

Кадр «Скролл событий» (`403:36101`) показывает, что карточка остаётся
**529** по высоте, а список прокручивается внутри и подрезается шапкой:
первое событие уезжает на y451 при шапке 408–480. В прототипе ровно это
и сделано — прокрутка 431 при содержимом 560. **Правок нет.**

### 5.3 Мобилка: механика другая, и она не сделана

Кадры `387:194192` (свёрнуто) и `403:107957` (развёрнуто), страница
«Мобилка» `223:98542`. Мобильную вёрстку решено пока не делать, поэтому
пункты остаются открытыми — но лежат здесь, а не в мобильном аудите.

| # | Что | Макет | Прототип |
|---|---|---|---|
| 5.5 | Высота строки | **60** | 56 |
| 5.6 | Сколько видно сразу | **четыре** события, карточка 343×240 | все десять |
| 5.7 | Как показать остальные | кнопка **«Показать ещё»** 14 SemiBold по центру | внутренняя прокрутка |
| 5.8 | Развёрнутое состояние | семь и больше событий, кнопка меняется на **«Свернуть»** | нет |
| 5.9 | Заголовок «События» | **18 SemiBold**, вне карточки | 20 SemiBold внутри карточки |
| 5.10 | Положение блока | между графиком и карточкой связей | справа от графика |

Главное: **на мобилке прокрутки внутри блока быть не должно совсем** —
список раскрывается и схлопывается кнопкой. Это отдельная логика,
а не адаптация десктопной, и сводить их в один компонент не получится.

## 6. Баннер PromBox

| # | Что | Макет | Прототип |
|---|---|---|---|
| 6.1 | Кнопка «Узнать больше» | 146×**36**, радиус **12** | 146×40, радиус 16 |

Баннер 404×160, радиус 24, фон `#7086fc`, белый текст 16 SemiBold и 14
Medium, картинка справа — совпадают.

## 7. Данные, а не вёрстка

- **Имя в заголовке.** В макете это идентификатор вида `ba3617558a88`,
  в прототипе — имя из списка (`Ant01`). Сделано намеренно: иначе переход
  из таблицы вёл бы на экран с другим именем. Решение за дизайнером —
  если в шапке должен быть идентификатор, его надо показывать и в таблице.
- **Подписи делений графика** — **исправлено**: деления, подписи осей
  и часы вынесены из svg в HTML, svg рисует только поле графика.
  Пропорции поля держит `aspect-ratio`, поэтому высота блока меняется
  с шириной как раньше, а кегль подписей постоянный — 12 и на 1920,
  и на 1440. Правка общая для всех графиков: Главная, деталка воркера,
  Доход, Мониторинг.

## 8. Не разобрано

- **Мобильная деталка** — `223:98542`: блоки и порядок разобраны
  в [audit-workers-mobile.md](audit-workers-mobile.md), раздел 3;
  события оттуда перенесены сюда, в раздел 5.
- Мобильные флоу деталки: настройка уведомлений (`405:110792`), группы
  и теги (`408:37525`), модель и заводской номер (`410:39063`).

## Сделано

Правки внесены 13.09.2026, замеры перепроверены на 1920 и 1440.

| Пункт | Что стало |
|---|---|
| 1.1 | Кнопка «назад» 48×48, радиус 16, фон `--ctrl`, иконка `Arrow Left` Linear |
| 1.2 | Перед именем логотип производителя 24 — и он пропадает, когда модель неизвестна, как в пустом кадре `224:4039` |
| 1.3 | Статус — точка 8 цвета статуса и подпись 16/20 SemiBold Primary |
| 1.4 | Блоки связей — белые карточки 48 с радиусом 16 |
| 1.5 | Подписи связей переведены на Tertiary |
| 1.6 | В каждом блоке связей иконка `Pen 2` 24 справа |
| 1.7 | Чипы тегов 32 с радиусом 12 и подложкой цвета самого тега |
| 2.1–2.4 | Сводка собрана акцентной плашкой 492×100 с белым текстом, зазор до строки 12 |
| 3.1 | Заголовки карточек переведены на Secondary |
| 3.2 | Шаг строк 36: строки встали на 272, 308, 344 — как в макете |
| 3.3 | Значок монеты 24 перед значением |
| 3.4 | Кнопка копирования заводского номера — флоу `403:36099` заработал |
| 4.1 | Две кнопки масштаба 48 рядом с календарём |
| 4.2 | Календарь 256 |
| 5.1–5.3 | События стали таймлайном: кружок 28 цвета статуса на 16 %, точка 10 внутри, линия 1×28 вниз, строка 56, время Tertiary |
| 6.1 | Кнопка баннера 146×36 с радиусом 12 |

Попутно: на 1440 контролы графика больше не переносятся — вместо этого
в две строки уходит заголовок, ровно как в кадре `230:5518`.

Не сделано из этого файла: пункты **5.5–5.10** (события на мобилке —
ждут решения по мобильной вёрстке), имя в заголовке из пункта 7 (вопрос
к дизайнеру) и пункт 8 (мобильная деталка и её флоу). Подписи делений
графика из пункта 7 сделаны.

## Второй заход (13.09.2026)

Перепроверил экран после правок — по всем пунктам 1–6 расхождений
не осталось. Нашлось одно новое:

| # | Что | Макет | Прототип |
|---|---|---|---|
| 9.1 | Заголовок «События» | 20/24 SemiBold | было **24/30** (общее правило `.card h2` перебивало `.wdh2`) — **исправлено** |

Блоки связей в шапке получились 284, 189 и 271 против 248 и 311
в макете — ширина считается по содержимому, а в макете она зафиксирована
под конкретные значения. На разных воркерах числа всё равно разъедутся,
поэтому оставляю как есть.

События ещё раз сверены с кадром «Скролл событий» (`403:36101`):
карточка 404×529, строка 56, внутренняя прокрутка 431 при содержимом
560 — совпадает.

## Третий заход: модалки деталки (13.09.2026)

Сверены две модалки, которые деталка открывает из шапки и из карточки
параметров. Обе собраны по макетам, но по геометрии не проверялись.

| # | Модалка | Макет | Прототип |
|---|---|---|---|
| 10.1 | Настроить уведомления (`223:100486`) | 550×**334** | 550×358 |
| 10.2 | Добавить заводской номер и модель (`223:100396`) | 550×**452** | 550×474 |

Причина у обеих одна и общая для всего прототипа: **подзаголовок
в макете лежит в шапке**, а у нас — в теле. `Modal HeaderA` — 92
по высоте (паддинг 24/16, заголовок 30, зазор 4, подзаголовок 18),
в прототипе шапка 80 плюс подзаголовок в теле с отступом 18.
Разбор и правка — в [audit-workers.md](audit-workers.md), пункт Ф2.

Что сошлось: ширина 550, крестик 40 кругом, поля 56, подвал 104
с двумя кнопками 247×56, состав полей и подписей, ссылки «База знаний»
и «Напишите нам», лимит и подсказки.

Ограничение максимальной высоты (`calc(100vh - 70px)` вместо `86vh`)
тоже общее — пункт Ф1 там же. На деталку оно не влияет: обе модалки
ниже порога.

## Порядок работ

Пункты 1–4 и 6 сделаны, ниже — что осталось.

1. **События на мобилке** (5.5–5.10) — самостоятельная правка, её можно
   сделать до общей мобильной вёрстки, когда решим по оболочке.
2. Мобильная деталка целиком (раздел 8) и её флоу.
3. Имя в заголовке (пункт 7) — после ответа дизайнера.
4. Подписи делений графика (пункт 7) — общая правка движка графиков
   для всех экранов сразу.
