# Аудит: «Воркеры» и деталка на мобилке, плюс события

Сверка с файлом 🔵 **Воркеры [Web]** — `8mvRUmxWj1tT96Sb7Ou13h`:
страницы «Мобилка» `173:125851` (список) и `223:98542` (деталка),
плюс кадры событий на десктопе. Замеры прототипа сняты при **ровно 375**
через iframe: эмуляция панели браузера уже 506 не опускается.
Дата сверки: 13.09.2026.

## Главное: мобильной вёрстки нет

Оба экрана на 375 — это десктопный макет, втиснутый в телефон.

| Экран | Ширина вьюпорта | Ширина документа | Элементов за краем |
|---|---|---|---|
| Воркеры | 375 | **506** | 71 |
| Деталка | 375 | **641** | 30 |

Главный виновник на обоих — шапка аккаунта `.hgroup` шириной **458**:
баланс 138 + уведомления 48 + аккаунт 146 + тема 78 не переносятся
и не схлопываются. Дальше по списку:

- **Таблица воркеров**: `table-layout:fixed` на 12 колонок в 343 —
  колонка «Наименование» ужимается до **24 пикселей**. `.tw` при этом
  не прокручивается (343/343), то есть таблица не уезжает вбок,
  а схлопывается в нечитаемую сетку.
- **График в деталке**: svg **311×142** вместо 375×498 из макета,
  деления наезжают друг на друга.
- **Заголовок страницы** остаётся 32 (в макете 20).
- **Плитки статусов** встают в один столбец по 359 (в макете — пять
  компактных плиток в строку).
- **Связи воркера** в деталке переносятся в три строки по 284
  и занимают 228 по высоте — в макете их на мобилке вообще нет наверху.

Всё это — не «поправить отступы», а отдельная мобильная вёрстка.

## 1. Оболочка: макеты рисуют приложение, а не сайт

В мобильных кадрах есть **статус-бар iOS 44**, шапка **72** и нижний
**таб-бар 375×95** с пятью кнопками. Прототип — веб-кабинет с сайдбаром,
который на 1150 просто исчезает, и ничем не заменяется.

Кроме того, состояния мобильного списка нарисованы **в трёх параллельных
секциях**: «Состояния Главного экрана воркеров» (`217:86902`),
«…(для апки)» (`309:83137`) и «…(без динамики)» (`311:93395`).
Наборы кадров в них совпадают.

**Вопросы дизайнеру:**
1. Какая из трёх секций канонична?
2. Нужен ли прототипу таб-бар и статус-бар, или на мобилке он остаётся
   сайтом и меню уезжает в бургер?

Без ответа на второй вопрос браться за мобилку нельзя: от него зависит
вся навигация, а не оформление одного экрана.

## 2. Список воркеров на 375 (`371:99056`)

Чего нет в прототипе, сверху вниз:

| # | Блок | Макет |
|---|---|---|
| 2.1 | Шапка | «Воркеры» **20 SemiBold** слева, монета **18 SemiBold** справа; кнопки «Серийные номера» и «Подключить воркер» из строки действий убраны |
| 2.2 | Сводка | карточка 343×**244**: акцентная плашка с 28 SemiBold, под ней сетка 2×2 — «За 5 мин», «За 1 ч», «Реджект за 24 ч», «Uptime за 24 ч», подписи **12 Medium**, значения **16 SemiBold** |
| 2.3 | Лента групп | подписи **14 SemiBold**, стрелка прокрутки 40 |
| 2.4 | Поиск и действия | поле «Поиск» 14 Medium и рядом **три кнопки 40** (фильтры, сортировка, экспорт) — на десктопе это кнопки с подписями |
| 2.5 | Плитки статусов | **пять компактных плиток в строку**: значение 14 SemiBold, подпись **10 SemiBold** («Низкий хэш» — сокращённо) |
| 2.6 | Таблица | **четыре колонки**: «Наименов.», «Хэш, 5 мин», «Хэш, 24 ч», «Реджект». Шапка 12 SemiBold, строки **12 Medium**, шаг 48 |
| 2.7 | Пагинация | своя, компактная |

Состояния, нарисованные отдельно: много воркеров, несколько, нулевое,
переключение на LTC, скролл ленты групп, выбор количества строк,
пагинация «мало страниц» и «одна страница», переход в деталку.

## 3. Деталка на 375 (`387:194192`)

**Порядок блоков другой, чем на десктопе:** сводка → показатели →
параметры → график → события → **связи отдельной карточкой внизу** →
баннер. В прототипе связи живут в шапке — на мобилке их там быть не должно.

| # | Блок | Макет |
|---|---|---|
| 3.1 | Шапка | 72: «назад» 40 слева, по центру имя **24 SemiBold** и под ним статус **12 SemiBold**, справа аватар 36 |
| 3.2 | Сводка | 343×188: плашка 32 SemiBold, под ней «За 5 мин» и «За 1 ч» — 12 Medium + **16 SemiBold** (на десктопе 18) |
| 3.3 | Показатели | карточка **без заголовка**: сразу строки 12 Medium / 16 SemiBold |
| 3.4 | Параметры | заголовок **16 SemiBold** и текстовая кнопка **«Изменить»**, а не иконка карандаша |
| 3.5 | График | 375×498, деления **10 SemiBold**, подписи осей 14 Medium, легенда 14 Medium |
| 3.6 | Связи | карточка 343×224 внизу: «Группы», «Теги», «Порог уведомлений» — подписи 12 Medium, значения 16 SemiBold, чипы тегов 14 |
| 3.7 | Баннер | 343×156 |

Отдельно нарисованы: пустое состояние, длинный номер и модель, много
групп и тегов, копирование номера с алертом, календарь, выбор периода,
BTC и LTC.

## 4. События

Разобраны отдельно и целиком — в [audit-worker-detail.md](audit-worker-detail.md),
раздел 5: там и десктопная часть (сделана), и мобильная механика
с «Показать ещё» (пункты 5.5–5.10, открыты).

Коротко: на десктопе список прокручивается внутри карточки 529 и это
совпадает с макетом, а на мобилке прокрутки быть не должно — четыре
события и кнопка раскрытия.

## 5. Не разобрано

Мобильных флоу в файле тринадцать секций, и ни один не разобран:
подключение, создание и удаление групп и тегов (по отдельности
и массово), привязка и отвязка, удаление воркеров, сортировка, поиск,
экспорт, фильтры, серийные номера. Каждый — со своими кадрами на 375.

Мобильные флоу деталки: настройка уведомлений (`405:110792`), группы
и теги (`408:37525`), модель и заводской номер (`410:39063`).

## Порядок работ

1. **Ответ дизайнера про оболочку** (раздел 1) — до него остальное
   делать бессмысленно.
2. Починить то, что ломается на любой узкой ширине независимо от
   решения: шапка аккаунта `.hgroup`, заголовок страницы, плитки,
   прокрутка таблицы вместо схлопывания.
3. События на мобилке — пункты 5.5–5.10 в
   [audit-worker-detail.md](audit-worker-detail.md). Правка компактная
   и самостоятельная, её можно сделать раньше общей мобильной вёрстки.
4. Список воркеров на 375 (раздел 2).
5. Деталка на 375 (раздел 3) с перестановкой блоков.
6. Мобильные флоу (раздел 5) — по тому же порядку, что и на десктопе.
