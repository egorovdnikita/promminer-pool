# Promminer Pool — интерактивный прототип

Правдоподобный прототип личного кабинета майнинг-пула **Promminer** для тестирования фич
и прогона сценариев, которые невозможно воспроизвести на проде (там пустой аккаунт без данных).

**Прод:** https://pool.promminer.ru/

## Что это

Vite + React + TypeScript, роутер — TanStack Router (файловый), стили — CSS дизайн-системы
плюс Tailwind v4. Сценарий приложения живёт в хэше ссылки, поэтому любую комбинацию
можно скинуть команде одним URL.

Прежняя однофайловая версия сохранена в [`archive/index.html`](archive/index.html):
она открывается двойным кликом без сборки и служит эталоном внешнего вида,
пока экраны переезжают в TSX.

## Как запустить

```bash
npm install
npm run dev
```

Откроется на http://localhost:5173. Сборка и проверка типов — `npm run build`,
просмотр собранного — `npm run preview`.

## Структура

```
src/
  main.tsx              точка входа
  routes/               маршруты: __root.tsx (каркас), index.tsx (Главная), $route.tsx (разделы)
  state/store.tsx       состояние сценария и интерфейса, обработка кликов, синхронизация с хэшем
  components/           каркас: Sidebar, Header, Coinbar, Footer, Modal, ScenarioPanel, Toasts
  legacy/prototype.js   движок из архива: данные, иконки, экраны, модалки — переезжает в TSX по частям
  styles/               app.css (дизайн-система), react.css, index.css
```

## Сценарии

Кнопка **⚙ Сценарии** в правом нижнем углу (или клавиша `S`) открывает панель переключателей.
Оси сценариев:

| Ось | Значения |
|---|---|
| Монета | BTC / LTC + DOGE |
| Наполнение данными | Норма / Пусто / Мало записей / Большие значения |
| Здоровье парка | Всё живо / Деградация / Авария |
| Роль | Владелец / Наблюдатель |
| Уровень рефералки | 5% / 10% / 15% / 20% / 25% |
| Верификация | Не пройдена / На проверке / Пройдена |
| Уведомления | 12 новых / 2 новых / Нет |
| Аккаунт | Основной / Суб-аккаунт |

Оси задаются в объекте `AXES` в начале скрипта — новый сценарий добавляется одной строкой.

## Разделы

- Главная
- Воркеры (список + карточка воркера)
- Финансы: Мои активы, Доход, Выплаты
- Отчёт о майнинге
- Инструменты: Калькулятор доходности, Калькулятор налогов
- Мониторинг (сводка по аккаунтам)
- Мои рефералы: Общая информация, Список рефералов, Реферальный доход, Реферальные выплаты
- Профиль, Безопасность, Уведомления, Суб-аккаунты, Наблюдатели, Верификация
- Вход / регистрация

## Дизайн

Токены, размеры контролов и типографика сняты с прод-сборки дизайн-системы —
точные значения и таблицы в [docs/prod-analysis.md](docs/prod-analysis.md).
Шрифт Gilroy (5 начертаний, woff2 с прода) лежит в `public/fonts` и подключается
из `src/styles/app.css`, поэтому прототип рендерится тем же шрифтом, что и продукт.
Иконки — Solar Bold Duotone из [💙 Дизайн-системы](https://www.figma.com/design/8rrL0jaovYD3jsrq7WF1L1/).
Есть тёмная тема — переключатель в хедере.
Список всех макетов — в [docs/context.md](docs/context.md).

## Документация

- [docs/context.md](docs/context.md) — исходное ТЗ, ссылки на макеты, договорённости
- [docs/prod-analysis.md](docs/prod-analysis.md) — разбор прода: роутинг, адреса, токены
- [docs/roadmap.md](docs/roadmap.md) — что сделано и что дальше

## Публикация

Прототип — статический SPA. `npm run build` кладёт сборку в `dist/`,
дальше её раздаёт любой статический хостинг с двумя условиями:

- **фолбэк на `index.html`** — адреса разделов настоящие (`/workers`,
  `/assets`), без правила «неизвестный путь → index.html» ссылка на раздел
  и перезагрузка дают 404;
- **корень домена** — около двадцати путей к картинкам и шрифтам записаны
  абсолютно (`/tier-5.png`, `url('/fonts/…')`) прямо в строках движка,
  Vite их не переписывает, поэтому раздача из подпапки их сломает.

### GitHub Pages

Ничего, кроме самого GitHub, не нужно. В репозитории есть workflow
`.github/workflows/pages.yml`: на каждый пуш в `main` он собирает проект
с `BASE_PATH=/promminer-pool/`, кладёт рядом `404.html` (копию
`index.html` — под фолбэк) и публикует.

Разово включить: **Settings → Pages → Source: GitHub Actions**. Дальше
пуш в `main` или запуск workflow вручную. Адрес —
`https://<логин>.github.io/promminer-pool/`.

Подпапка учтена: `vite.config.ts` берёт базу из `BASE_PATH`, роутер
получает её через `basepath`, а пути к картинкам в движке собираются
от константы `A` (`import.meta.env.BASE_URL`).

### Vercel

Настройки лежат в `vercel.json`. Разовая публикация:

```
vercel login      # один раз, откроет браузер
vercel --prod
```

Без аккаунта можно выложить временную копию: `vercel deploy --temporary`,
её потом забирают себе по ссылке.

Сценарий живёт в хэше ссылки, поэтому ссылки со сценариями работают
на любом хостинге.

## Пароль на вход

Прототип закрыт паролем: при первом открытии показывается экран «Войти
в прототип», дальше ключ лежит в `localStorage` и на этом устройстве
больше не спрашивается. Ошибка — «Пароль неверный», «Забыли пароль?»
ведёт в телеграм.

Сменить пароль:

```
npm run gate
```

Скрипт спросит пароль в терминале (эхо погашено), посчитает SHA-256
и перепишет `GATE_HASH` в `src/gate.ts`. Сам пароль никуда не пишется.
Дальше — коммит и пуш; все, кто уже входил, спросятся заново.

**Это щеколда, а не защита.** Проверка идёт в браузере, репозиторий
публичный — и код проверки, и хэш видны любому, кто откроет исходники.
Экран отсекает случайного посетителя, но не того, кто захочет войти.
Если нужен настоящий доступ по списку — Cloudflare Pages с Access:
вход по одноразовому коду на почту, бесплатно, код прототипа при этом
не меняется.
